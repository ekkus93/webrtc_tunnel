//! Config loading/validation test suite.
//!
//! The shared fixtures live in [`support`]; the test functions are grouped by concern into
//! the sibling modules below.

mod support;

mod data_plane;
mod forwards;
mod loading;
