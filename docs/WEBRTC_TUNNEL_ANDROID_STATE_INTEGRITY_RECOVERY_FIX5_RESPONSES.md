# FIX5 Spec + TODO Responses\n\n## Questions for the User\n\n### Q1: Should `pausedByPolicy.get()` be required in `handleNativeFailureAfterStartup`?\nThe spec says "require it" as safer default but notes the pending retry itself proves policy pause.\n\nA: \n\n### Q2: For P0-003 production wiring, where should `NetworkPolicyManager` get its reporter?\nOptions: (a) pass reporter into `AppDependencies` constructor, (b) create manager in `TunnelForegroundService.onCreate()`, (c) add a lazy reporter callback to `AppDependencies`.\n\nA: \n\n### Q3: For P0-004, should `Log.w` include a throwable or not?\nIncluding a throwable preserves stack trace but risks leaking the original message. Not including it loses stack trace but is safer.\n\nA: \n\n### Q4: For P1-005, is it acceptable to fake `ConfigRepository` to force rollback failure, or should we use a real file-permission scenario?\nFaking is simpler and more reliable in tests.\n\nA: \n\n### Q5: P0-004 blocks P0-003 ordering\nThe interface change (`cause: Throwable?` -> `message: String`) must happen before production wiring, since the new reporter signature won't compile against the old interface. Should P0-004 be implemented first?\n\nA: \n\n### Q6: `processCommand` catches specific exceptions\nFor P1-003 adding a generic `catch (error: Throwable)`, the existing catches for `IllegalArgumentException`, `IllegalStateException`, `IOException` should remain before the generic catch to preserve current behavior. Is this correct?\n\nA: \n\n### Q7: All existing NetworkPolicyManager tests use `NoopNetworkPolicyEventReporter`\nThese will need updating after P0-004 changes the interface. Should these be updated in FIX5?\n\nA: \n\n### Q8: P0-002 test hook alternatives\nThe suggested test shape references `service.testHooks.submitLifecycleCommandIfPossible(...)` which doesn't exist in the codebase. Should the fix use observable behavior (asserting no restart via bridge state/start counts)?\n\nA: \n\n---\n\nFill in the `A:` lines and share back when ready.\n