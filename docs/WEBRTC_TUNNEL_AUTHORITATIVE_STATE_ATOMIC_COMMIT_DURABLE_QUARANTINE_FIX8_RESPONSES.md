# FIX8 Responses — WEBRTC_TUNNEL_AUTHORITATIVE_STATE_ATOMIC_COMMIT_DURABLE_QUARANTINE_FIX8 (SPEC + TODO)

Fill in each `A:` line, then share this file back (or paste the answers). Implementation will not begin until these are answered. Items 1 and 4 already carry decisions captured during the review; confirm or change them.

---

1. Q: Pacing. FIX8 is 16 tasks / 10 P0 blockers / ~250 checkboxes / ~130 named tests — a multi-day effort. The TODO defines Stages A–F but, unlike FIX7, gives no explicit stop points. Should I (a) checkpoint per stage — stop and report for review after each of Stage A–F; (b) checkpoint after Stage B and Stage D only (FIX7's exact 2-checkpoint cadence); or (c) run straight through with one commit per task, no stops, until all P0/P1/P2 are done or I hit a blocker? Default absent direction: (b).
   A: Choose **(c)**. Run straight through with one scoped commit per TODO task and do not stop for approval between stages. After Stages B and D, append a concise checkpoint section to the implementation report containing the current SHA, completed tasks, validation executed, deviations, and open risks, then continue automatically. Stop only for a genuine blocker that prevents a binding invariant from being implemented safely, an unexpected baseline conflict that materially changes the design, or a required destructive action that is not authorized. A test failure, lint failure, or implementation defect is work to fix—not a reason to stop and request routine approval.

2. Q: Validation environment. Signoff (P2-002-C/D/E/F) requires gradle, cargo, Docker E2E, Android emulator E2E, and green GitHub Actions CI on the exact signoff SHA. The FIX7 reviewer could run none of these (no network, no cargo/rustc, gradle wrapper download failed). Which can THIS session actually execute: `cargo` (fmt/clippy/test); Android `./gradlew`; Docker real-broker E2E; Android emulator/ADB (for setup-wizard, live metered-to-unmetered, and service-recreation-while-quarantined E2E); outbound network for `git push` / CI? Anything unavailable will be marked `NOT RUN: <reason>`, never PASS-by-inspection (per spec §10).
   A: Do not inherit or assume the FIX7 reviewer sandbox's limitations. Before the first production-code change, perform and record an explicit environment preflight for **this Claude Code session**. At minimum probe `cargo --version`, `rustc --version`, `./gradlew --version`, `docker info`, `docker compose version`, `adb version`, `adb devices -l`, the available Android SDK/emulator images, `git remote -v`, and authenticated GitHub access (`gh auth status` when `gh` is installed). Execute every validation category whose preflight succeeds. If an emulator is not already running but the SDK and a usable AVD are available, boot it and run the required emulator tests. Push and wait for CI only when the repository remote and authentication permit it. Record each probe command, exit status, and decisive output in the implementation report. Any unavailable category must be written exactly as `NOT RUN: <specific reason>` and must not be converted into PASS by code inspection, prior FIX7 evidence, or an unrelated lower-level test. Unavailable Docker/emulator/network validation does not block implementing and locally validating the code, but final signoff must remain explicitly incomplete for that category.

3. Q: Broker-secret permissions (P0-008-A). `android.system.Os.chmod`/`Os.stat` are not faithful under JVM/Robolectric unit tests. Confirm the intended design: an injectable permission enforcer/verifier — real `Os.chmod`/`Os.stat` `0600` enforcement on device/emulator, with a faked enforcer for JVM unit tests that exercises the `broker_secret_permissions_failed` path and the owner-only verification. Is that the intended approach?
   A: **Yes.** Introduce a small injectable permission boundary, for example `BrokerSecretPermissionEnforcer`, whose production Android implementation uses `Os.chmod(path, 0o600)` followed by `Os.stat(path)` and verifies that no group/other permission bits remain. JVM unit tests must inject deterministic fakes for success, chmod failure, stat failure, and verification mismatch. The repository must not report persistence success until permission enforcement and verification have succeeded. A permission failure must produce the fixed/redacted `broker_secret_permissions_failed` result and participate in the surrounding exact rollback transaction; it must not leave a newly replaced secret accepted as authoritative. Add at least one emulator/instrumentation test using the real implementation when an emulator is available. Do not fall back to ignored `File.setReadable`/`setWritable` Boolean results and do not weaken the contract merely to accommodate Robolectric.

4. Q: Static enforcement mechanism (P2-001). The repo CLAUDE.md policy forbids `@Suppress`/baselines/threshold-lowering. P2-001 wants permanent rules forbidding production `runCatching`, bare authoritative `File.delete()`, ignored `mkdirs`/`setReadable`/`setWritable`, setup-controller authoritative mutation calls, `unix_ms:0`, `snapshot.bytes ?: ByteArray(0)`, and config writes inside candidate/workspace scope. Should enforcement be (a) JVM test / source-scan guards that scan production sources and fail on forbidden patterns with exact allowlisted exceptions, or (b) genuine custom detekt rules compiled into the build? Review decision captured: (a) test/source-scan guards.
   A: Confirm **(a): JVM test/source-scan guards**. Keep the guard rules narrow, path-aware, and backed by permanent positive and negative fixtures. Allowlist only exact, reviewed files or exact syntactic forms where an exception is genuinely necessary; do not use broad directory exclusions, marker comments as the sole proof of safety, `@Suppress`, baselines, or threshold changes. Existing type-aware detekt/Android Lint checks such as `@CheckResult` remain in force. A custom detekt plugin is not required for FIX8 unless a listed invariant cannot be enforced reliably by the approved source-scan tests and existing type-aware rules; document such a blocker rather than silently weakening the check.

5. Q: Doc-path discrepancy. The spec (line 6), TODO (P2-002-A, lines 1514–1518), and manifest reference the FIX7 review and FIX8 manifest at `docs/review-source/WEBRTC_TUNNEL_FIX7_CODE_REVIEW_2026-07-21.md` and `docs/review-source/WEBRTC_TUNNEL_FIX8_HANDOFF_MANIFEST.md`, but the pull placed both at `docs/` root. Signoff P2-002-A checks the `review-source/` paths. Resolve by (a) `git mv` both files into `docs/review-source/` to match the documents, or (b) leave them at `docs/` root and amend the spec/TODO/manifest references?
   A: Confirm **(a)**. Use `git mv` to place both files at `docs/review-source/WEBRTC_TUNNEL_FIX7_CODE_REVIEW_2026-07-21.md` and `docs/review-source/WEBRTC_TUNNEL_FIX8_HANDOFF_MANIFEST.md`. Keep the spec and TODO references unchanged because those are the intended canonical paths. Update any manifest inventory or checksums affected by the move, and add a signoff check proving no duplicate stale copies remain at `docs/` root.

6. Q: Baseline confirmation. The reviewed baseline was `webrtc_tunnel-master_2607211131.zip` (no `.git`). Current HEAD is `6eb1085`. The FIX7 review references `OfferCoordinator.kt`/`AppDependencies.kt`, so it postdates the OfferCoordinator split, but since then `status.rs`, `p2pctl/main.rs`, and `TransactionalResetCoordinatorTest.kt` were also split (none in FIX8's material scope). Confirm FIX8 should target current HEAD `6eb1085` and treat the review's line numbers as approximate anchors (adapt to current code, do not paste over changed code) — per the manifest's own guidance.
   A: **Confirmed.** Target current repository HEAD `6eb1085` as the FIX8 implementation baseline and record its full SHA before modifying code. Treat review line numbers, filenames, and snippets as semantic anchors rather than patch offsets. Inspect the current production path and related tests before every edit, preserve legitimate post-review refactors, and adapt the FIX8 design to the current split files. Do not copy an older whole-file implementation over newer code. If the actual starting HEAD is no longer `6eb1085`, record the new SHA and diff it against `6eb1085`; continue when the difference is only the expected FIX8 document/path preparation, but stop and document a blocker if unrelated production changes materially invalidate a binding FIX8 design assumption.

7. Q: Nullable diagnostic timestamp — breaking schema change (SPEC §5.8 / §3.8, P0-010). Changing `AndroidLogEvent.unix_ms` from `u64` to `Option<u64>` changes the Rust→JNI/C-ABI→Kotlin serialization schema. Spec §11 explicitly authorizes dropping zero-timestamp compatibility. Confirm no out-of-tree consumer reads the old `unix_ms:0` JSON that would break, i.e. the change is safe to make unconditionally.
   A: **Confirmed.** The in-repository Android/JNI path is the authoritative consumer, and FIX8 may change the schema unconditionally. Change Rust `AndroidLogEvent.unix_ms` to `Option<u64>` and the corresponding serialized/Kotlin model to nullable, update every in-tree producer, parser, fixture, and test in the same task, and represent an unavailable timestamp as `null`—never `0`. No backward-compatibility shim that reintroduces zero is permitted. If implementation discovers a concrete out-of-tree consumer contract rather than merely speculating that one might exist, record it as a blocker for review instead of silently preserving the unsafe fallback.

8. Q: Evidence artifacts to create. Implementation must author `docs/review-source/WEBRTC_TUNNEL_FIX8_IMPLEMENTATION_REPORT.md` (required Claude Code output, P2-002-A) and write inventory logs under `.aiworkflow/logs/fix8/` (does not exist yet; not gitignored). Confirm both should be created and committed as part of the work (the `.aiworkflow/logs/fix8/` initial inventories are the first concrete step of P0-001 per the TODO's "Required initial inventories").
   A: **Confirmed.** Create and commit `docs/review-source/WEBRTC_TUNNEL_FIX8_IMPLEMENTATION_REPORT.md` and the `.aiworkflow/logs/fix8/` evidence directory. The initial inventory logs are the first concrete P0-001 work and must record the baseline SHA, exact command, execution date/time, exit status, and relevant output. Add final inventories and validation summaries as FIX8 progresses. Commit concise text evidence only—no Gradle caches, binaries, APKs, Docker layers, emulator images, raw private keys, broker passwords, tokens, complete secret-bearing configuration files, or unnecessarily revealing absolute user paths. Redact sensitive values while preserving enough context to prove the check. The implementation report must link each task to its commit SHA, tests, deviations, unresolved `NOT RUN` items, and final signoff SHA.

---

Please fill in the blank `A:` lines (and confirm items 4 and 5) and share back. No spec, TODO, or source files will be modified until then.
