# WebRTC Tunnel FIX8 Implementation Report

**Status:** In progress
**Baseline HEAD (recorded before first production change):** `050cb060a82e7a63d164cb1a8a57cbdfeb15b0ac`
**Target project:** `webrtc_tunnel`
**Binding documents:**
- `docs/WEBRTC_TUNNEL_AUTHORITATIVE_STATE_ATOMIC_COMMIT_DURABLE_QUARANTINE_FIX8_SPEC.md`
- `docs/WEBRTC_TUNNEL_AUTHORITATIVE_STATE_ATOMIC_COMMIT_DURABLE_QUARANTINE_FIX8_TODO.md`
- `docs/review-source/WEBRTC_TUNNEL_FIX7_CODE_REVIEW_2026-07-21.md`
- `docs/WEBRTC_TUNNEL_AUTHORITATIVE_STATE_ATOMIC_COMMIT_DURABLE_QUARANTINE_FIX8_RESPONSES.md` (binding answers)

This report records, per task: the commit SHA, the exact negative-path tests added, commands run with results, deviations, and any `NOT RUN` items. Checkpoint summaries are appended after Stage B and Stage D (pacing answer 1(c): run straight through, checkpoint in-report, do not stop for routine approval).

---

## Environment preflight (this session)

Probed before the first production change. Unlike the FIX7 review sandbox, this session has a full toolchain.

| Probe | Result |
|---|---|
| `cargo --version` | `cargo 1.94.1 (29ea6fb6a 2026-03-24)` — AVAILABLE |
| `rustc --version` | `rustc 1.94.1 (e408947bf 2026-03-25)` — AVAILABLE |
| `./gradlew --version` | `Gradle 8.7` — AVAILABLE |
| `docker info` | OK — AVAILABLE |
| `docker compose version` | `v5.1.3` — AVAILABLE |
| `adb version` | `1.0.41` — AVAILABLE |
| `adb devices -l` | `emulator-5554 device product:sdk_gphone64_x86_64` — EMULATOR RUNNING |
| `git remote -v` | `git@github.com:ekkus93/webrtc_tunnel.git` — AVAILABLE |
| `gh auth status` | Logged in to `ekkus93` — AVAILABLE |

Consequence: cargo, gradle, Docker E2E, emulator E2E, and CI push are all executable this session. No validation category is pre-emptively `NOT RUN`. Any category that later fails to run for a specific reason will be recorded here as `NOT RUN: <reason>` and will keep final signoff explicitly incomplete for that category (never PASS-by-inspection).

---

## Setup actions (pre-P0-001)

- Moved `WEBRTC_TUNNEL_FIX7_CODE_REVIEW_2026-07-21.md` and `WEBRTC_TUNNEL_FIX8_HANDOFF_MANIFEST.md` from `docs/` to `docs/review-source/` (`git mv`) so signoff path checks (P2-002-A) pass against the canonical paths. No stale copies remain at `docs/` root (verified).
- Created `.aiworkflow/logs/fix8/` and captured the TODO's required initial inventories:
  - `initial-head.txt`, `initial-status.txt`
  - `setup-authoritative-mutation-inventory.txt` (7 hits: SetupIdentityController ×2, SetupForwardsController ×2, ForwardsViewModel ×2, ImportExportService ×1)
  - `unsafe-api-inventory.txt` (30 hits)
  - `config-preference-inventory.txt` (187 hits)
  - `quarantine-inventory.txt` (30 hits)
  - `test-timing-inventory.txt` (29 hits)
  - `rust-diagnostic-fallback-inventory.txt` (jni_bridge.rs:206 `"unix_ms":0` production; c_abi.rs:160 recent_logs failure path; log_bridge.rs:206 `unix_ms: 0` is `#[cfg(test)]` only)

---

## Task log

### Setup / relocation — commit `dcbbf65`
`git mv` FIX7 review + FIX8 manifest into `docs/review-source/`; created `.aiworkflow/logs/fix8/` inventories + `environment-preflight.txt`; added this report skeleton. Evidence logs scanned for secret-value patterns (clean — source line references only).

### P0-001-A — SetupIdentityDraft private-byte holder — commit `fae7aa9`
- Added `viewmodel/SetupIdentityDraft.kt`: non-`data` `SetupIdentityDraft` + `DraftIdentityReplacement`, `internal`. Wipes previous bytes on `replace`/`clear`; `copyForSave()` returns an independently-owned copy; `replace` requires non-empty/non-blank fields.
- Added `SetupIdentityDraftTest.kt` (5 tests): `replaceWipesPreviousPrivateBytes`, `clearWipesPrivateBytesAndDropsReplacement`, `copyForSaveReturnsIndependentCopyThatDoesNotAffectDraft`, `copyForSaveIsNullWhenEmpty`, `replaceRejectsEmptyOrBlankFields`. Confirmed the test failed to compile first (`Unresolved reference 'SetupIdentityDraft'`), then passed 5/5 after implementation.
- Focused: `testDebugUnitTest --tests '*SetupIdentityDraftTest'` → tests=5 failures=0. ktlint (main+test) + detekt (all source sets) PASS.

### Sequencing note (documented reorder, permitted by TODO §0)
Reading `SetupSaveController.validateAndCommit`/`commitSetup` showed **P0-001-C (draft-only forwards) is coupled to P0-004-D (setup `Forwards` transactional stage)**. The wizard currently persists `forwards.json` eagerly via `ForwardsRepository.upsertWithReceipt/deleteWithReceipt`; the final setup transaction renders *enabled* forwards into `config.toml` but has no `Forwards` stage to write `forwards.json`. Removing the eager write in isolation would (a) leave `forwards.json` unpersisted by setup and (b) break existing forwards tests — violating the green-commit rule. Per spec §3.2 ("Forwards is a real transactional stage; it may not be committed by SetupForwardsController before Review save"), P0-001-C and P0-004-D will land together. P0-001-B (draft-only identity generate/URI/path import) and P0-001-D (final-save draft resolution) can still proceed first, since the import-path save resolution is already draft-shaped (`fromImport`).

### P0-001-B/D/E (identity) — commit `6a390ef`
- `SetupIdentityController` (now `internal`): generate / URI import / path import all validate → canonicalize → `SetupIdentityDraft.replace(...)`; **no `storeEncryptedIdentity`**, no `canonicalPublicIdentity.orEmpty()`, no `generated.peerId ?: input.localPeerId` fallback. New `requireCanonicalIdentity()` fails closed (fixed messages) on any missing canonical field. `importPublicIdentityFromUri` also lost its `runCatching` (explicit try/catch, a P1-001-C item done early).
- `SetupSaveController` (now `internal`): `resolveSaveIdentity` checks the draft first (`copyForSave()`, `fromImport=true`); the save-owned copy is wiped by `validateAndCommit`'s existing `finally`. Successful save clears the draft; a failed save retains it for retry. **The `importIdentityPath` re-read branch is retained as a fallback in this commit** and is removed in the P0-001-D follow-up (test migration required).
- `SetupViewModel`: owns `internal val identityDraft`; wipes it on `cancel()` and `onCleared()`. Removed the private `updateState` helper (folded into `applyState`) to stay within detekt TooManyFunctions after adding `onCleared`.
- Visibility: the two controllers + their `SetupViewModel` accessor vals became `internal` (single-module app) so they can take the `internal` draft without an exposure error; `ConfigurationMutationIntegrationTest`'s direct `SetupSaveController(...)` construction updated with the new param.
- Tests: renamed the two "persists identity" tests to assert **draft-only, nothing persisted**; added `SetupWizardNoIdentityMutationTest` (9 tests): generate/URI/path "does not mutate live identity" (byte-exact file snapshots), `missingCanonicalPublicIdentityFailsWithoutFallback`, `missingGeneratedPeerIdFailsWithoutPriorPeerFallback`, `replacingDraftIdentityWipesPreviousPrivateBytes`, `setupViewModelClearWipesDraftPrivateBytesOnCancel` (holds the live byte ref, asserts zeroed), `successfulFinalSaveWipesAndClearsDraft`, `failedFinalSaveRetainsRetryableDraft`. Added an `internal` byte-observation seam (`SetupIdentityDraft.peekLivePrivateBytesForTest`, spec §8).
- Redaction note: I initially wrapped the identity-import failure messages in `SensitiveDataRedactor.redactText`, but its identity-path regex over-redacts the bare word "identity" and mangled a safe validation message. Reverted to FIX7's verbatim behavior here; proper boundary redaction is the dedicated **P1-001-C** task ("SetupIdentityController uses raw exception/native messages").
- Validation: focused `SetupWizardNoIdentityMutationTest` 9/9; `SetupViewModelTest` 36/36, `SetupSaveControllerTest` 17/17, `ConfigurationMutationIntegrationTest` 6/6, integration/authorized-keys/workspace suites green; ktlint (main+test) + detekt (all source sets) PASS; **full `testDebugUnitTest` PASS** (1m11s).

### P0-001-D follow-up (import-path re-read removal) — commit `c3f3a07`
- `SetupSaveController.resolveSaveIdentity()`: removed the `current.importIdentityPath.isNotBlank()` re-read branch entirely (was reading the file a second time at save, a TOCTOU window) — now draft-first, then stored-identity fallback only, matching the spec's required order. Dropped `resolveSaveIdentity`'s now-unused `current` parameter and the file-scoped `importPrivateIdentity()` helper + its now-unused `readPrivateIdentityFile` import (would have failed detekt `UnusedPrivateMember`/ktlint unused-import otherwise — no suppression used).
- **New finding, fixed in-scope**: removing the save-time re-read also removed its *incidental* secret-redaction side effect (`SaveError(..., redact = true)` routed through `SensitiveDataRedactor.redactText`). Without it, `SetupIdentityController`'s own `importIdentityFromPath`/`importIdentityFromUri` onFailure handlers assigned `it.message` raw to `errorMessage` — a live secret-leak regression, and also literally P0-001-B's own checklist item ("Redact native/file error messages before assigning UI state"), not merely deferred P1-001-C scope.
  - Root cause of my earlier revert (previous entry): `SensitiveDataRedactor.redactText`'s identity-path rule is *intentionally* over-broad (matches the bare word "identity" anywhere, documented/tested in `SensitiveDataRedactorTest.identityRuleAlsoMatchesThePlainWordInProse` as acceptable for diagnostic-log export) — wrong tool for a short user-facing validation message.
  - Fix: added `SensitiveDataRedactor.redactSecretValues()` — the structured-secret-field/PEM-block/bearer-auth/MQTT-userinfo passes only, refactored out of `redactText` (which now composes `redactSecretValues` + the protocol/identity-path rules, unchanged behavior, all existing `SensitiveDataRedactorTest` cases still pass). `SetupIdentityController`'s three onFailure handlers now use `redactSecretValues` — protects real secrets (`password=...`) without mangling benign prose containing "identity".
- Migrated 5 test call sites (`SetupViewModelTest` ×2, `SetupSaveAuthorizedKeysTest`, `SetupValidationWorkspaceIntegrationTest`, `SetupSaveControllerTest` ×2 incl. the `wizardReachingConfigWrite` helper) to call `identity.importIdentityFromPath()` after `setImportIdentityPath(...)` so the draft is actually populated before save (Robolectric + `Dispatchers.Main.immediate`/inline-`Unconfined` dispatchers execute the launched coroutine synchronously here, confirmed by the passing runs — no artificial await needed at these call sites).
- Updated `AllViewModelFailureRedactionTest`'s setup-identity redaction case to exercise `identity.importIdentityFromPath()` directly (the real redaction boundary now) instead of the removed save-time branch.
- Added the TODO-named test `pathFileReplacementAfterValidationCannotChangeCommittedIdentity` to `SetupWizardNoIdentityMutationTest`: imports, then replaces the file on disk *and* the bridge's canned validation result before save, and asserts the committed public identity is the original — proving no re-read.
- **Two pre-existing flaky tests discovered (NOT caused by this work — independently reproduced against the true pre-FIX8 baseline commit `050cb06` via a throwaway `git worktree`, both files/domains untouched by any FIX8 commit so far):**
  - `SetupSaveControllerTest.plaintextIdentityIsWipedOnCancellation` — fails when run in isolation (`--tests` filtered to just this method) but passes as part of its full class or the full suite. Order/shared-state dependent; reproduced failing on both `050cb06` and current HEAD when run alone.
  - `TunnelForegroundServiceInitializationRaceTest.startWhileExactlyInitializingDoesNotCallNative` — failed once during a full `./gradlew check` run (release variant), passed on immediate rerun in isolation. Consistent with a genuine race in a race-focused test, not this task's concern.
  - Neither is fixed here (out of scope for P0-001/identity work; both are exactly the class of CI-nondeterminism P1-004-C is scoped to address). Recorded for that task and for final signoff's "three consecutive full unit reruns" requirement (spec P2-002-C) — if either recurs during signoff, P1-004-C must root-cause it before signoff, per the hard rule against flaky/unchecked validation.
- Validation: focused re-run of all 9 affected test classes (SetupViewModelTest, SetupSaveControllerTest, SetupSaveAuthorizedKeysTest, SetupValidationWorkspaceIntegrationTest, ConfigurationMutationIntegrationTest, SetupIdentityDraftTest, AllViewModelFailureRedactionTest, SetupWizardNoIdentityMutationTest, SensitiveDataRedactorTest) — all green. Full `testDebugUnitTest` PASS (1m42s). Full `./gradlew check` PASS on rerun (ktlint + detekt + Android lint + debug/release unit tests, zero suppressions).

### P0-001-C — make setup forwards a pure draft — commit `4bbd63e`
- **Found late, during FIX8 P2-001's audit of what static enforcement needs to cover**: this task was never actually landed. The sequencing note above deferred it to "land with P0-004," and the "Still open in P0-001" note below correctly flagged it — but P0-004 (setup's transactional Forwards stage, which already existed and already took `access.forwards()` as its authoritative source at final commit) came and went without anyone circling back to fix the *live-edit* path, so `SetupForwardsController.upsertForward()`/`deleteForward()` kept calling `ForwardsRepository.upsertWithReceipt`/`deleteWithReceipt` directly — committing to the real `forwards.json` immediately during setup, contradicting P0-001's own CRITICAL-1 finding and the byte-exact-abandonment requirement. All of P0-001's checkboxes had been left unchecked in the TODO the whole time (the report above only ever recorded A/B/D/E, never C), so this was invisible until an explicit line-by-line TODO audit. Flagged to the user before starting further Stage E work; user confirmed implementing it now rather than deferring further.
- Both `upsertForward()` and `deleteForward()` now compute the candidate list and validate it exactly as before (`upsertForward` still calls `deps.forwardsStore.validateForwards`), but write only to `access.setForwards(...)` — the wizard's own in-memory draft. Neither calls `ForwardsRepository` at all anymore. `refreshForwardsBaseline()` (loads baseline into the draft) and `cancel()` (discards draft, reloads baseline) were already correct — only the interactive edit path was wrong.
- Edits now also gate on `access.loadState() is SetupLoadState.Ready`, matching every other setup action (P1-001) — this guard previously lived inside `ForwardsRepository.upsertWithReceipt`'s own `blockedMutationReason` check, which no longer runs during a live edit.
- Replaced `deleteForwardFailurePreservesErrorAndDoesNotClaimSuccess` (forced a real disk-write failure via a read-only files dir — no longer possible to trigger since edits don't write to disk) with the P0-001-E named tests: `setupWizardForwardDeleteDoesNotMutateLiveForwardsOrConfigBeforeFinalSave`/`setupWizardForwardUpsertDoesNotMutateLiveForwardsOrConfigBeforeFinalSave` (byte-exact `forwards.json` + presence-exact `config.toml`, plus the authoritative repository's own in-memory list untouched), `abandoningSetupWizardLeavesEveryAuthoritativeFileByteExact` (identity.enc/identity.pub/authorized_keys/forwards.json/config.toml/setup_input.json all snapshotted after the ViewModel's own baseline-load settles, then compared after generating an identity + editing a forward + `cancel()`), and a strengthened `failedFinalSaveWipesAttemptCopyButRetainsRetryableDraft` (renamed from `failedFinalSaveRetainsRetryableDraft`; now also proves the retained draft is genuinely usable via an actual successful retry, not just present and non-null).
- Validation: full `./gradlew check` PASS (`--rerun-tasks`); two consecutive full `testDebugUnitTest` runs green. `cargo fmt --all --check` + `cargo clippy --workspace --all-targets` PASS (no Rust changes).
- All P0-001 A/B/C/D/E checkboxes and Acceptance now ticked in the TODO, cross-referencing the original identity-side commits (`fae7aa9`, `6a390ef`, `c3f3a07`) which were correctly implemented but never had their TODO checkboxes marked — this report's own entries for them (above) were the only record of that work until now.

### P0-002 — actual-owner admission + preference-write serialization — commits `b9ac1be`, `b48eb7d`
- **P0-002-A**: `ConfigurationMutationCoordinator` rewritten from `Mutex.tryLock()` + separately-set `active` (HIGH-3: a losing racer could read `active == null` in the acquisition gap and report its own operation as the busy owner) to an atomic `AtomicReference<ActiveConfigurationMutation?>` CAS. Acquisition and owner-publication are now one indivisible `compareAndSet`. Hardened beyond the spec's own illustrative snippet: if a losing CAS is followed by a read that also finds `active == null` (owner released in the interim), the whole attempt *retries* rather than ever returning a null-derived or invented `Busy` owner — closing a residual gap in the spec's suggested code. Added `ConfigurationOperation.PreferenceMutation`.
- **P0-002-B**: `SettingsViewModel.savePreferences` and `NetworkPolicyViewModel.savePreferences` now acquire `PreferenceMutation` admission around the full read/modify/write; `Busy` maps to durable `configuration_operation_busy` naming the real owner, matching every other operation's existing pattern.
- **P0-002-C**: `SetupSaveController.commitSetup` no longer re-reads preferences via `loadPreferences()` — it now reuses the exact `prefs` object `validateAndCommit` already read for rendering/validation, passed through as a parameter. (Note: `SetupPersistenceCoordinator.captureSnapshot()` still calls the injected `loadPreferences` lambda once, for its own pre-transaction rollback snapshot — a distinct, legitimate purpose, not the redundant render/commit duplication this task targets.)
- Tests: `ConfigurationMutationCoordinatorTest` +3 (`busyAdmissionDuringOwnerPublicationAlwaysReportsActualOwner` — concurrent stress proving mutual exclusion holds under real contention; `sameOperationTypeCannotClearAnotherOwnerToken`; `fatalErrorReleasesTokenAndPropagatesSameInstance` — asserts `===` same-instance propagation). `ConfigurationMutationIntegrationTest` +3 end-to-end (`settingsPreferenceMutationBlocksConcurrentSetupSaveDurably`, `setupSaveBlocksConcurrentNetworkPreferenceMutationDurably`, `concurrentPreferenceWriteCannotBeLostBySetupRollback` — proves a busy-rejected write is not lost, just deferred, and commits once admission frees). `SetupViewModelTest` +1 (`setupValidationAndCommitUseSamePreferenceSnapshot`, asserting `loadPreferences` call count == 1, down from 2).
- **Split `SetupViewModelTest.kt`** (776 lines) into itself (565) + new `SetupIdentityActionsTest.kt` (251, the `SetupIdentityController` generate/import/importPublic test group) — triggered by detekt `LargeClass` after these additions, following this session's established large-file-splitting pattern.

#### Incident: full-suite hang, root-caused and fixed (not a signoff blocker, but recorded per spec §10 honesty requirements)
The first version of `busyAdmissionDuringOwnerPublicationAlwaysReportsActualOwner` spawned 2000 concurrent coroutines on the shared, process-wide `Dispatchers.IO` pool. Running the full suite afterward **hung indefinitely** (confirmed via two independent runs, `jstack`-diagnosed both times) — not in that test itself, but in a later, completely unrelated test (`HomeViewModelTest.homeViewModelAllowMeteredTemporarilyDoesNotPersistPreference`, stuck in `runBlocking` on a real AndroidX DataStore `.edit{}` write). Root cause: three of my new `ConfigurationMutationIntegrationTest` cases released their `CompletableDeferred` gate at the end of the test but never awaited the now-unblocked ViewModel coroutine's actual completion before the test method returned — leaving a real, in-flight DataStore write dangling on `viewModelScope` past the test boundary, contending with later tests' access to the same Robolectric Application's DataStore file. Fixed by:
1. Reducing the stress test to 250 iterations on a small dedicated `Dispatchers.IO.limitedParallelism(8)` (moved into its own `InjectDispatcher`-compliant helper, `runBlockingOnBoundedRealDispatcher`) instead of the suite-shared IO pool.
2. Adding the missing post-release completion wait to `settingsPreferenceMutationBlocksConcurrentSetupSaveDurably` (the one test actually missing it — the other two already waited correctly).
Verified via `jstack` thread dumps at each stage (captured the exact hang point both times) and confirmed fixed by three consecutive full `testDebugUnitTest` runs (1m10s–2m2s each) plus a full `./gradlew check` (3m21s), all green.

#### Third pre-existing flaky test discovered
During the third consecutive full-suite rerun, `TunnelForegroundServiceOrderingTest.nativeFailureConsumesPendingPolicyRetryAndResumesExactlyOnce` failed once, then passed cleanly in isolation on rerun. This is a different test/domain (Tunnel foreground service lifecycle) than either of the two flaky tests already recorded after P0-001-D (`SetupSaveControllerTest.plaintextIdentityIsWipedOnCancellation`, `TunnelForegroundServiceInitializationRaceTest.startWhileExactlyInitializingDoesNotCallNative`) and is untouched by any FIX8 commit so far — confirmed unrelated to this work. Recorded as a third P1-004-C item; not fixed here (out of scope for P0-002).

> **Note on report continuity:** this report was not updated turn-by-turn through P0-003–P0-006 (a session context-compaction gap); the four entries below were written retroactively from git history and the surviving task record once the gap was discovered, at the same detail level as the entries above. Every commit SHA, test name, and validation claim below was verified against the actual repository state (`git show`, targeted test reruns) before being recorded — none is reconstructed from memory alone.

### P0-003 — exact config/setup-input APIs, atomic writes, attempted-stage rollback — commits `55d877d`, `24b9aa2`
- `ConfigFilesSnapshot` pairs exact byte snapshots of `config.toml` and `setup_input.json`, captured under one `fileMutex` boundary (renamed from `writeMutex`, now serializing both files) — replacing the previous separate, unlocked, String/Boolean-derived `configExisted`/`configContents`/legacy `SetupInputSnapshot` fields (CRITICAL-3).
- `atomicReplaceBytesWith` (`ConfigAtomicWrite.kt`) is the one generic same-directory temp-plus-atomic-move byte primitive, unifying config's own writer and the generic byte-replacement helper setup-input/restores use; `Files.createDirectories` (checked) replaces an ignored `mkdirs()`; catches every ordinary `Exception` (not just `IOException`), so a `SecurityException` from a denied file operation returns a failure instead of propagating uncaught.
- `ConfigRepository` takes an injectable `AtomicConfigFileOps` (`internal` constructor, defaulted to `RealAtomicConfigFileOps`) so tests can force deterministic write/move/cleanup failures or prove `fileMutex` serialization without a flaky filesystem permission trick.
- `SetupPersistenceCoordinator.persist()` tracks both `applied` and `attempted` stages: a stage added to `attempted` *before* its own apply call means a failure that mutates its destination before failing is still rolled back (CRITICAL-2), and even a stage that fails before any mutation gets an idempotent rollback attempt.
- `readConfig`/`hasEncryptedIdentity`-style no-arg accessors converted to properties (not member functions) to stay under detekt's `TooManyFunctions` threshold without suppression — the pattern reused for every subsequent P0-003–P0-006 task.
- Tests: `AtomicReplaceBytesTest.kt` (atomic-write-before-move exactness, `SecurityException` handling), `ConfigRepositoryTest.kt` (snapshot exactness across absent/empty/non-UTF-8 bytes, capture-vs-writer serialization), `SetupPersistenceCoordinatorExactBytesTest.kt` (new file — stage-fails-after-a-real-move exact-byte rollback, secret-wiping on every exit path).
- Validation: focused suites green; ktlint (main+test) + detekt (all source sets) PASS; full `testDebugUnitTest` PASS. Docs commit `00e6aa4` checked off P0-003's boxes.

### P0-004 — exact forwards transaction state + one complete setup transaction — commits `54ad896`, `9423860`, `6fc9e49`
- **A/B/C** (`54ad896`): `ForwardsStore` gains `captureExactSnapshot()`/`restoreExactSnapshot()` (implemented by `ForwardsConfigStore` via the same `ExactFileSnapshot`/`atomicReplaceBytes` primitives config/setup-input already use, so `forwards.json` gets an identical absent/present-empty/exact-bytes distinction). `ForwardsRepository` adds a mutex-guarded `ForwardsTransactionSnapshot` with `captureForTransaction()` (fails unless the baseline is `Ready`), `replaceForTransaction()`, and `restoreForTransaction()`. Every ordinary mutation now saves through `selfRestoringSave`: captures exact prior bytes before saving and, on any save failure — including a cleanup-only failure raised after the destination was already atomically moved — restores those exact bytes, composing a self-restore failure into a typed `ForwardsSaveRollbackIncompleteException` rather than discarding it.
- **E** (`9423860`): `IdentityRepository.restoreIdentityPairSnapshot`/`restoreAuthorizedKeysSnapshot` (narrow, single-purpose restores) added alongside the existing full-triplet `restoreStorageSnapshot`, so a rollback of one setup stage never touches the other's file.
- **D/F/G** (`6fc9e49`): `SetupPersistenceStage.Forwards` added (after Preferences, before Config); `SetupPersistenceRequest` gains a required `forwards: List<ForwardConfig>` field (the full wizard draft including disabled entries — no default, so a caller can't silently wipe forwards to empty); the three optional identity/security stage triggers grouped into `SetupOptionalChanges` to stay under detekt's `LongParameterList` threshold. `SetupPersistenceCoordinator` now captures/applies/rolls back Forwards alongside identity/broker-secret/config, using P0-004-E's stage-specific identity restores instead of the full-triplet one.
- New `SetupPersistenceCoordinatorForwardsTest.kt`: Forwards commits before Config; a Forwards failure/cancellation rolls back itself and every earlier stage; a later Config failure/cancellation restores forwards' exact bytes/list/load-state; a Forwards rollback failure is reported not hidden; Identity/AuthorizedKeys rollback use their distinct restore members.
- **Genuine regression found and fixed in this task**: three `ConfigurationMutationIntegrationTest` setup-save integration tests never refreshed the harness's `ForwardsRepository` before use; the coordinator's new Forwards-stage snapshot capture (requiring a `Ready` baseline) failed before the tests' gated `ConfigRepository` fake was ever entered, hanging the test on a `CompletableDeferred` that could no longer complete. Fixed by adding the missing `refresh()` calls — the first instance of a "missing forwards refresh" bug class that recurred (and was fixed the same way) in P0-005 and P0-006 below.
- Validation: focused suites + full `testDebugUnitTest` green at each of the three commits; ktlint + detekt PASS throughout. Docs commit `726b535` checked off P0-004's boxes.

### P0-005 — config import and forward-activation cleanup-before-commit transactions — commits `191193f`, `42ce50c`, `299398c`
- **A/B** (`191193f`): `ConfigRepository.replaceConfigTransactionally` — capture-attempt-restore for `config.toml`, the config-side counterpart to `ForwardsRepository.selfRestoringSave` — captures exact prior bytes, attempts the write, and restores those bytes if the write fails (including on cancellation, restored under `NonCancellable`); a restore failure composes into `ConfigReplaceRollbackIncompleteException`. `ImportExportService`'s config import now commits through `replaceConfigTransactionally` only *after* `withCandidateFile`'s cleanup has already succeeded, instead of writing inside the candidate block — previously a cleanup-only failure could report "import failed" while `config.toml` had already been overwritten (CRITICAL-4).
- **C/D** (`42ce50c`): new `ForwardConfigurationCoordinator` (stages `Forwards`, `Config`) — `ForwardsViewModel` now builds the proposed forwards list and renders+native-validates its candidate config entirely in memory (zero mutation) before any authoritative write, then commits both forwards and config atomically through the coordinator, replacing the previous mutate-forwards-then-roll-back-via-receipt-if-config-fails pattern.
- **Genuine regression found and fixed in this task**: `ForwardsTransactionSnapshot`/`restoreForTransaction` (added in P0-004) lacked the revision-mismatch protection `rollbackReceipt` already had (P1-002) — a coordinator rollback could silently overwrite a newer concurrent forwards mutation. Fixed by adding `capturedRevision` to the snapshot and a `stageWasApplied` parameter to `restoreForTransaction`, so a live-revision mismatch is detected and the restore is skipped (reported as `ForwardConfigurationRollbackStageResult.Skipped`, surfaced to the user as "forwards changed again") rather than silently clobbering the newer state.
- **Diagnosed and fixed a genuine dispatcher-hang bug** introduced while wiring the coordinator into `ForwardsViewModel`: two separate `withContext(ioDispatcher)` blocks (one for render/validate, one for the coordinator's `apply()`) caused an extra Main↔IO dispatch hop that hung under Robolectric's paused-looper test harness; merged into one `withContext` boundary for the whole activation, matching the single-hop shape every other ViewModel mutation in this codebase already used. Root-caused via temporary file-based diagnostic logging (`jstack` proved unreliable — Gradle test-worker PIDs cycled too fast to attach) before being fully removed.
- **F** (`299398c`): added the remaining named exact-byte tests — `ForwardConfigurationCoordinatorTest.kt` (new file: config-failure/post-move-cleanup-failure/cancellation exact restores, rollback-continues-after-one-failure, rollback-incomplete-lists-every-failure), plus `configImportWritePostMoveFailureRestoresPreviousConfigBytes` and the ViewModel-level candidate-cleanup/validation-failure exact-byte tests.
- Validation: full `testDebugUnitTest` + full `./gradlew check` green after each commit, `testDebugUnitTest` reconfirmed on a second consecutive full run per this session's hang-history discipline. Docs commit `f672879` checked off P0-005's boxes.

### P0-006 — exact reset, corruption-tolerant, attempted-stage rollback — commits `0621d75`, `0dbefda`
`TransactionalResetCoordinator` had not been brought up to the attempted-before-apply/exact-snapshot standard the other three coordinators (Setup, ForwardConfiguration, and P0-003/004/005's primitives) already met. Four real defects found and fixed:
- **Corrupt setup JSON blocked reset entirely** (HIGH-5): `captureSnapshot()` called `configRepository.loadSetupInputResult().getOrThrow()` before every reset, so a corrupt `setup_input.json` — the exact state a user needs reset to repair — permanently blocked it. Capture is now raw bytes only (`captureSetupInputFileSnapshot`, already exact/injectable), no parsing.
- **Config was captured lossily**: `configContents` (a UTF-8 `String`) was converted `.toByteArray()`, corrupting non-UTF-8 bytes on any snapshot/restore cycle. Fixed via a new `ConfigRepository.captureConfigSnapshotForReset` property (raw bytes, `captureExactFileSnapshot`) — and the restore side had the *same* bug (a `String(bytes, UTF_8)`-converted `writeConfigAtomically` call), found and fixed in the follow-up commit (`0dbefda`) once a dedicated non-UTF-8 test caught it; restore now delegates to the existing `ConfigRepository.restoreConfigSnapshot` byte-exact primitive.
- **Snapshot failure was unconditionally reported as `ResetStage.Config`** regardless of which component's capture actually failed. `SnapshotCaptureException` now carries the real stage (`Config`/`SetupInput`/`Forwards`).
- **Attempted-vs-applied ordering bug** (CRITICAL-2, the same class fixed for every other coordinator this cycle): stages were added to the rolled-back set only *after* their own apply succeeded, so a stage that changed its destination and only then reported failure would never be rolled back. Fixed identically to P0-003/004/005: `attempted` gains a stage before its apply runs, `applied` (only genuine successes) drives each stage's `stageWasApplied` restore parameter.
- Setup-input's own reset now goes through `saveSetupInputAtomically` (Result-returning, mutex-serialized) instead of the non-atomic `saveSetupInput`; forwards' capture/restore switched from a plain re-serializable `List<ForwardConfig>` to the same `ForwardsTransactionSnapshot`/`restoreForTransaction` primitive every other transaction's Forwards stage uses (the now-fully-unused `ForwardsRepository.restoreForTransactionalReset` and `ConfigRepository.deleteConfigFileForTransactionalReset` were removed, not left as dead code).
- Rewrote the five existing tests across `TransactionalResetCoordinatorTest.kt`/`TransactionalResetCoordinatorRollbackReportingTest.kt`/`TransactionalResetHardeningTest.kt` that asserted the old (now-fixed) "corrupt setup input blocks reset" and "Forwards excluded from its own rollback" behaviors, to assert the corrected behavior instead; updated every stale fake overriding `saveSetupInput`/`configContents`/`deleteConfigFileForTransactionalReset` to target the new seams (`saveSetupInputAtomically`/`captureConfigSnapshotForReset`/`restoreConfigSnapshot`) — several of these went silently inert before being caught by full-suite reruns.
- New `ForwardConfigurationCoordinatorTest.kt`-style file `TransactionalResetPostMoveAndCancellationTest.kt` covers: non-UTF-8 config exactness, post-move failures for the SetupInput and Forwards reset stages specifically, per-stage cancellation (Config/SetupInput/Forwards), and secret-wipe coverage across success/failure/cancellation/fatal-error outcomes.
- **Fourth pre-existing flaky test encountered** (same class as the three already on file): `SetupSaveControllerTest.plaintextIdentityIsWipedOnCancellation` failed once in a full-suite run, passed cleanly in isolation and on full-suite rerun — reconfirmed unrelated to this task (untouched files/domain), not fixed here.
- Validation: full `./gradlew check` PASS and full `testDebugUnitTest` PASS (two consecutive runs) after each commit. Docs commit `7112393` checked off P0-006's boxes.

---

## Checkpoint 1 — after Stage B (per RESPONSES item 1(c))

**Current HEAD at checkpoint:** `7112393`
**Stages complete:** A (P0-001, P0-002), B (P0-003, P0-004, P0-005, P0-006) — all TODO checkboxes in both stages ticked with commit SHAs, cross-checked against actual repository state (not assumed from memory).

**Completed tasks (commit SHA map):**
| Task | Commits |
|---|---|
| Setup/relocation | `dcbbf65` |
| P0-001-A | `fae7aa9` |
| P0-001-B/D/E | `6a390ef` |
| P0-001-D follow-up | `c3f3a07` |
| P0-002 | `b9ac1be`, `b48eb7d` |
| P0-003 | `55d877d`, `24b9aa2` |
| P0-004 | `54ad896`, `9423860`, `6fc9e49` |
| P0-005 | `191193f`, `42ce50c`, `299398c` |
| P0-006 | `0621d75`, `0dbefda` |

**Validation executed:** every task above closed with a focused test rerun for its own change plus a full `./gradlew check` (ktlint + detekt, all source sets, zero suppressions + Android lint + debug/release unit tests) and — for tasks touching shared coordinator/dispatcher code (P0-004 onward) — at least two consecutive full `testDebugUnitTest` runs, per this session's hang-history discipline. All green at HEAD.

**Deviations from the TODO's illustrative code:**
- P0-006-A's "reset uses `ConfigRepository.captureFilesSnapshot()`" was implemented as two separate calls (a new `captureConfigSnapshotForReset` property for config, the existing `captureSetupInputFileSnapshot` for setup-input) rather than the single combined `captureFilesSnapshot()` — required so a capture failure can be attributed to the actual failing component (P0-006-A's own accurate-stage-attribution requirement), which the combined call cannot distinguish internally. Documented in the TODO's P0-006-A checkbox note.
- No other deviations from the TODO/spec's binding requirements; illustrative code snippets were adapted to this codebase's established property-not-function (detekt `TooManyFunctions`) and attempted-vs-applied idioms throughout, consistent with prior Stage A/B tasks.

**Known open risks carried forward (not blockers):**
- Four pre-existing flaky tests identified across Stage A/B, none caused by FIX8 work and none yet root-caused (that is P1-004-C's explicit scope): `SetupSaveControllerTest.plaintextIdentityIsWipedOnCancellation` (recurred twice), `TunnelForegroundServiceInitializationRaceTest.startWhileExactlyInitializingDoesNotCallNative`, `TunnelForegroundServiceOrderingTest.nativeFailureConsumesPendingPolicyRetryAndResumesExactlyOnce`. Each passes in isolation and on rerun; none has recurred three times in a row at any single checkpoint's signoff-style validation, so none currently blocks progress, but P1-004-C must still root-cause all three before final FIX8 signoff.
- This report itself had a continuity gap (P0-003–P0-006 went unrecorded turn-by-turn and were backfilled retroactively at this checkpoint, per the note above) — a process risk to watch for in Stages C–F, not a code risk.

**Continuing automatically per RESPONSES item 1(c)** — proceeding to Stage C (P0-007 onward) without waiting for review.

---

## Stage C progress

### P0-007 — fail-closed identity restore, coherent reads — commit `ac21369`
- **A**: every `snapshot.bytes ?: ByteArray(0)` fabrication replaced with `requireNotNull` naming the logical file; every unchecked `File.delete()` replaced with checked `Files.deleteIfExists`; unified into one `restoreFileFromSnapshotOrThrow` primitive shared by the pair-commit rollback and the setup-transaction snapshot restore.
- **B**: verified already satisfied by P0-004-E (`restoreIdentityPairSnapshot`/`restoreAuthorizedKeysSnapshot`/`restoreStorageSnapshot`, each attempted independently, reasons already redacted).
- **C**: new `IdentityRepository.readStoredIdentityMaterial` (encrypted+public pair read as one coherent snapshot under `storageLock`) and `readPrivateIdentityPlaintextOrNull` (single-file coherent existence-check-and-read) close the TOCTOU window every caller previously left between a separate `hasEncryptedIdentity` check and a separate unsynchronized read. Decryption happens outside the lock. Wired into `SetupSaveController.resolveSaveIdentity`/`resolveStoredIdentity`, `ForwardsViewModel.readIdentityBytesOrThrow`, `ImportExportService.importConfigContent`. No call site was found holding `IdentityRepository`'s own lock across a native validation call to begin with (investigated via a dedicated read-only agent pass before implementing) — the fix is the new coherent-read methods themselves releasing the lock before returning, which `identityReaderDoesNotHoldLockDuringNativeValidation` verifies directly (a concurrent `storeEncryptedIdentity()` completes quickly after a coherent read has already returned, proving no lock is held across a simulated slow validation step).
- **D**: checked `Files.createDirectories` for `exportPrivateIdentity`/`exportPublicIdentity`; the atomic-move-fallback log no longer passes the raw `Throwable` to `Log.w`. The path-based export methods have no current ViewModel caller (the wired export flows are URI-based, already redacted via `ImportExportViewModel`'s existing `io.run` boundary) — noted in the TODO rather than fabricating a redaction boundary for an uncalled method.
- Added an injectable `deleteIfExists` seam mirroring the existing `atomicReplace` one (constructor-ordered so no existing trailing-lambda call site broke), plus all 8 named P0-007-E tests.
- Validation: full `./gradlew check` + two consecutive full `testDebugUnitTest` runs green (one pre-existing flaky-test recurrence, see Known open risks).

### P0-008 — broker-secret permissions, fatal-safe cleanup, checked directory ops — commits `c9b21ee`, `d2dbe3c`
- **A**: new injectable `BrokerSecretPermissionEnforcer` replaces the ignored `File.setReadable`/`setWritable` Boolean calls with real `Os.chmod`+`Os.stat` verification (`0600`), applied to the temp file before the plaintext secret is written into it and re-verified on the destination after the atomic move; `restore()` enforces/verifies the same way. Any failure surfaces as the fixed, redacted `BrokerSecretPermissionException("broker_secret_permissions_failed")`.
  - **Discovered during this task**: `Os.chmod`/`Os.stat` do not behave reliably under Robolectric — the real enforcer made every existing `BrokerSecretRepositoryTest`/`SetupPersistenceCoordinatorTest` call site using the default enforcer fail uniformly. Fixed by adding `RecordingPermissionEnforcer`/`FailingPermissionEnforcer` JVM fakes (a new shared fixtures file) and injecting them at every call site that doesn't override `atomicReplace` entirely (the ones that do were already bypassing the real enforcer and needed no change). Real `Os`-level behavior is explicitly **NOT RUN** this session (no emulator/instrumentation test executed) — recorded as an open acceptance-criterion gap, not silently marked done.
- **B**: `withCleanupComposition` now runs cleanup even when the primary block throws a fatal `Error`, not just an ordinary `Exception` — previously an `Error` skipped the `try/catch` entirely and skipped cleanup (and any leftover secret-bearing candidate file) too. **Deviation from the TODO's illustrative code**: implemented as three explicit catches (`CancellationException`, `Error`, `Exception` — jointly exhaustive over `Throwable`) instead of one literal `catch (Throwable)`, because this repo's `detekt.yml` was deliberately configured (FIX6 Q11) to flag `catch (Throwable)` specifically; the three-clause form keeps that rule meaningful everywhere else in the app without a suppression or a new config exemption, while preserving the exact same rethrow-unchanged guarantee. Reduced the rewritten function to one literal `throw` statement (a computed `toThrow` value with `check(outcome is ScopedOutcome.Value)` for the success path) to stay under detekt's `ThrowsCount` threshold without an exemption either.
  - **Genuine regression caught by the existing test suite before commit**: the first version of this rewrite wrapped cleanup in a try/catch to handle a fatal `Error` from cleanup, but that catch also caught `CancellationException` from cleanup and incorrectly wrapped it into `CandidateCleanupException` instead of letting it propagate unchanged — broke `ImportExportServiceTest.configImportCancellationBeforeCommitLeavesConfigExact`. Fixed by special-casing `cleanupFailure is CancellationException` alongside `is Error` before falling through to the generic wrap.
- **C**: `createCandidateFile`/`withTemporaryDirectory` and every remaining secret-adjacent parent-directory creation (identity replace, forwards save, setup-input save, diagnostics/config export, `ForwardsViewModel`'s candidate temp file) switched from ignored `mkdirs()` to checked `Files.createDirectories`; `deleteCandidateFileSafely`/`deleteDirectoryRecursivelySafely` broadened from `IOException` to `Exception`; `ForwardsConfigStore.saveForwards` likewise broadened plus its parent-directory creation checked. Audited for bare `File.delete()`: the one remaining reference (`ForwardsConfigStore`'s injectable `deleteTempFile: (File) -> Boolean = File::delete`) already checks its Boolean return at the call site, so left as-is rather than changing its exception-vs-Boolean contract without a driving test.
- Added the remaining 5 P0-008-D tests (`d2dbe3c`) after fixing 3 detekt `UnreachableCode` findings the first draft introduced (a `try { ...; null } catch { ... }` pattern where the compiler proved the trailing `null` unreachable once the block unconditionally throws — restructured to a `var caught` pattern instead).
- Validation: full `./gradlew check` + two consecutive full `testDebugUnitTest` runs green after each commit (one pre-existing flaky-test recurrence at the first commit, zero at the second).

### P0-009 — application-scoped runtime quarantine (CRITICAL-6/HIGH-10) — commit `3643841`
- New `NativeRuntimeSafetyState` (`data/NativeRuntimeSafetyState.kt`, internal type, matching `BrokerSecretPermissionEnforcer`/`StoredIdentityMaterial`'s existing internal-type pattern) replaces `TunnelForegroundService`'s two service-local `AtomicBoolean`s (`nativeRuntimeUncertain`/`nativeStopVerified`) with one application-scoped owner in `AppDependencies` — a `synchronized`-guarded `MutableStateFlow<NativeRuntimeSafetySnapshot>` (`quarantined`/`stopVerified`/`code`/`message`/`generation`), with four named transitions (`markStartAttempted`/`quarantine`/`markObservedStopWithoutRecovery`/`markVerifiedExplicitStop`) — the actual CRITICAL-6/HIGH-10 fix, since the old service-local fields silently reset to "not quarantined" on every Android-driven service recreation.
- `TunnelRepository`'s primary constructor gained `safetyState: NativeRuntimeSafetyState` as its first parameter (`internal constructor`, same pattern as `BrokerSecretRepository`) so `stop()`/`refreshStatusResult()` can read/write it. `stop(explicitVerifiedStop: Boolean = false, ifGenerationUnchanged: Long? = null)`: verification always reads raw native truth via a new `NativeRuntimeStatusDto.reportsVerifiedStop` property, deliberately ignoring any pre-existing quarantine — otherwise a quarantined runtime could never be verified stopped, making explicit-STOP recovery (the entire point of quarantine) impossible. Only `stopServiceWork()`'s call (`explicitVerifiedStop = true`) may clear quarantine on a verified stop (`markVerifiedExplicitStop()`); every other caller (`pause()`, `pauseForPolicy()`, the destroy fallback, `cleanupUnverifiedStart`'s start-verification cleanup) uses the default `false`, calling `markObservedStopWithoutRecovery()` instead, which never touches `quarantined` — this is what actually fixes the bug: `onDestroy()`'s fallback previously cleared `nativeRuntimeUncertain.set(false)` unconditionally on any successful fallback stop, regardless of whether quarantine came from an unrelated earlier failure.
- `refreshStatusResult`'s and `stop`'s own status commits both apply a new `TunnelStatus.withNativeRuntimeSafetyOverlay` property as the last step, overlaying a quarantined Error state over any mapped native status (including a bare `Stopped`) using the **fixed canonical** code/message — never `safety.code`/`safety.message` (the per-failure diagnostic). **Genuine regression caught before commit**: a first version used `safety.code` in the overlay, which let a concurrent status poll race `TunnelForegroundService.enterNativeRuntimeQuarantine`'s own two `repository.setLocalError` calls (the narrower per-failure code, then the canonical one) and leave the narrower code as the final published `lastError.code` — caught by an intermittent failure of the new `nativeStatusRefreshCannotOverwriteQuarantineWithConnected` test; fixed by hardcoding the canonical string in the overlay, matching RESPONSES item 2's invariant exactly.
- `handleRetryPolicyResume`'s `.getOrNull()` silent-return bug fixed exactly per the TODO's suggested mapping: `getOrElse` + `invalidatePendingPolicyRetry()` + `reporter.publishErrorSafely(code = "native_runtime_recovery_required", ...)` — previously this was the one guarded call site that discarded a blocked-retry diagnostic silently, unlike every other guard failure (`startOffer`/`resume`/`allowMeteredForSessionAndStart`/`handlePolicyAllowed`).
- **P0-009-E generation guard (disclosed, narrow scope)**: `NativeRuntimeSafetyState.markObservedStopWithoutRecoveryIfGenerationUnchanged(expectedGeneration)` skips the transition if the shared generation has advanced past what the caller observed; `onDestroy()`'s fallback captures the generation immediately before calling `repository.stop(ifGenerationUnchanged = ...)`. This guards only the specific call site the review findings named (a stale service instance's destroy-time fallback); the native `bridge.stop()` call itself still fires unconditionally in that path (full cross-instance mutual exclusion over the shared native bridge is out of this task's scope), and `quarantine()`/`markVerifiedExplicitStop()` were not given an equivalent generation-guarded variant (not named by P0-009-F's test list).
- New `TunnelForegroundServiceRuntimeSafetyRecreationTest.kt`: all 10 named P0-009-F tests, each constructing two real `ServiceController<TunnelForegroundService>` instances sharing one Robolectric Application/`AppDependencies` graph (never a locally mutated Boolean), per the TODO's explicit instruction. `pendingPolicyRetryQuarantineGuardFailureIsDurableAndVisible`/`reporterFailureCannotPreventSharedQuarantineTransition` construct `ServiceCoordinatorOperations` directly (internal, same technique the pre-existing `UnverifiedStartContextTest` already uses) and inject a custom `NotificationController(notifyAction = ...)` — an existing constructor seam — to observe or fail the reporter deterministically; both needed an explicit `notificationsAllowedProvider = { true }` override since this Robolectric environment's default target SDK (35) denies `POST_NOTIFICATIONS` by default, which would otherwise make `NotificationController.show()` no-op before `notifyAction` is ever invoked (discovered via an initial failing run of `pendingPolicyRetryQuarantineGuardFailureIsDurableAndVisible`).
- `UnverifiedStartContext`'s own `nativeStopVerified: AtomicBoolean` field removed — now redundant, since `TunnelRepository.stop()` (wired as `context.stop`) already records the observed-stop-without-recovery transition internally; `UnverifiedStartContextTest.kt` updated to drop the now-removed field/assertions while keeping its meaningful ones (mandatory-stop-under-`NonCancellable`, cancellation-not-laundered, quarantine-on-cleanup-failure, quarantine-survives-reporter-failure).
- **Fifth pre-existing flaky test discovered and confirmed via baseline isolation** (not caused by this task): `TunnelForegroundServiceVerificationTest.startVerificationCleanupFailureEntersRuntimeQuarantine` failed intermittently during this task's second full-suite rerun. Verified pre-existing (not a P0-009 regression) by `git stash`-ing all P0-009 changes and rerunning the same test class in isolation 5 times against the true pre-P0-009 baseline: it failed on the 4th of 5 baseline runs too. Root cause (by inspection, not fixed here — out of scope, same class as the four already-tracked flaky tests): the test's own `waitForCondition` polls a weaker predicate (`serviceState == Error && lastError != null`) that can already be true after `enterNativeRuntimeQuarantine`'s *first* `setLocalError` call (the narrower per-failure code), then immediately asserts the *canonical* code in a separate statement — a real OS-thread-scheduling race between those two adjacent, non-suspending statements, unrelated to any P0-009 logic. Recorded as the fifth P1-004-C item.
- Validation: full `./gradlew check` PASS; full `testDebugUnitTest` green on the first run, one flaky-test recurrence (see above, confirmed pre-existing) on the second consecutive run, clean on a third rerun.

---

## Stage D — P1 (high-priority integration and boundary hardening)

### P1-001 — serialize setup-local operations, redact boundaries, load asynchronously (HIGH-4/HIGH-11/MEDIUM-5) — commit `2019fe3`
- New `SetupOperationCoordinator` (mirroring `ConfigurationMutationCoordinator`'s atomic-owner-token design) replaces the two duplicated per-controller `launchBusy` implementations (`SetupIdentityController`, `SetupForwardsController`) and `SetupSaveController`'s complete absence of any busy tracking at all. `SetupDraftOperation` (`BaselineLoad`/`IdentityAction`/`ForwardEdit`/`ValidationNavigation`/`FinalSave`) is the shared admission taxonomy; `isBusy` is now stamped inside `SetupViewModel.applyState` (the one write path every controller already used) from the coordinator's real ownership, never toggled independently.
- **Two genuine gaps found and fixed via this task's own new tests, before commit**: (1) the first version only re-published `isBusy` at the very end of `runGuarded`, so a caller observing published state (not `operations.isBusy` directly) saw `busy=false` for an operation's entire duration until it finished — fixed by also publishing immediately upon acquiring admission, as the first statement inside the admitted block. (2) a `CancellationException` propagating out of `tryRun` skipped `runGuarded`'s final re-stamp entirely (it was a plain sequential statement after the `when` expression, never reached on early exceptional return) — fixed by moving it into a `finally`, so a cancelled action's admission-release is always reflected in published `isBusy`, not left stuck `true`.
- Final save nests admission: `SetupSaveController.saveAndApplyConfigInternal` now runs entirely inside `operations.runGuarded(FinalSave) { ... }`, and the pre-existing `deps.configurationMutationCoordinator.tryRun(SetupSave)` call (FIX7 P0-001-C) is unchanged inside that block — setup-local admission first, then the application-scoped one, never the other order.
- New `SetupLoadState` (`Initializing`/`Ready`/`Failed`, mirroring the existing `ForwardsLoadState` pattern one layer down in `ForwardsRepository`) replaces `SetupViewModel.init`'s previously-synchronous `loadStoredSetupInput` call (a direct, blocking `setupInputFile.readText()` + JSON decode on whatever thread constructs the ViewModel — the main thread in production) with `loadSetupWizardBaseline`, IO-dispatched inside the `BaselineLoad`-guarded init coroutine. `SetupIdentityController.loadStoredIdentity()`/`SetupForwardsController.refreshForwards()` (both already async) gained direct-suspend variants (`loadStoredIdentityBaseline`/`refreshForwardsBaseline`) called from within that same guarded sequence rather than through the coordinator again — the coordinator is not reentrant, so re-entering it from inside an already-admitted `BaselineLoad` block would self-deadlock/self-reject.
- `goNext()` and `SetupSaveController.saveAndApplyConfigInternal` both now block (with a visible, fixed error) while `loadState` is not `Ready` — a corrupt/unreadable saved setup-input file is now a durable `setup_draft_load_failed`, no longer silently treated as "no saved draft" (the prior behavior showed a transient error but still let the user proceed with a blank draft).
- Two remaining raw/unredacted native-validation messages fixed: `resolveRemotePublicIdentity`'s `validated.message` and `generateIdentity`'s `generated.message`, both now through `SensitiveDataRedactor.redactText`. `importPublicIdentityFromUri`'s "removes `runCatching`" requirement was already satisfied before this task (its own comment already anticipated P1-001-C).
- `cancel()` calls the coordinator's `invalidate()` (a `staleBefore` watermark over the operation-id sequence) before resetting state, so a still-in-flight operation's eventual completion — the only way a stale result could reach published state under reject-on-overlap admission — checks its own captured id against that watermark and skips publishing if the wizard was reset in the meantime.
- New `SetupDraftOperationCoordinationTest.kt`: all 7 named P1-001-D tests. The no-main-thread-I/O tests use `realIoTestDispatchers()` to force a genuine thread hop, observing `loadState == Initializing` at the instant the constructor returns. The overlap test drives both sides directly through `operations.runGuarded` rather than through `SetupForwardsController.upsertForward`'s own `viewModelScope`-hopping launch, whose real-dispatcher scheduling did not reliably interleave with an already-parked `viewModelScope` coroutine in this harness — same underlying coordinator call either way.
- All pre-existing setup tests (`SetupViewModelTest`, `SetupIdentityActionsTest`, `SetupSaveControllerTest`, `SetupSaveAuthorizedKeysTest`, `SetupWizardNoIdentityMutationTest`, `SetupValidationWorkspaceIntegrationTest`, `ConfigurationMutationIntegrationTest`, `AllViewModelFailureRedactionTest`) continue to pass unchanged, including the four that construct `SetupViewModel` and assert on state synchronously right after — `inlineTestDispatchers()`'s Unconfined io/default plus `viewModelScope`'s Main.immediate happen to still resolve the whole `BaselineLoad` sequence eagerly in that environment.
- **Sixth pre-existing flaky test discovered**: `TunnelForegroundServiceRuntimeSafetyRecreationTest.reporterFailureCannotPreventSharedQuarantineTransition` (a P0-009 test, unrelated to P1-001 — different subsystem entirely) failed once during this task's full-suite run, passed 3/3 in isolation and cleanly on a full-suite rerun; recorded as a new P1-004-C item, not fixed here.
- Validation: full `./gradlew check` PASS; two consecutive full `testDebugUnitTest` runs green (one unrelated flaky-test recurrence on the first, confirmed pre-existing via isolation, clean on the second).

### P1-002 — make application initialization genuinely exactly-once (HIGH-7) — commit `f97c043`
- `AppInitializationCoordinator.start()`'s candidate job used the default eager `CoroutineStart`, so it began running `initialize()` immediately on `launch` — before the `compareAndSet` that picks a winner ever ran. Two concurrent callers could each launch a job that actually executed `initialize()` concurrently; the loser's `job.cancel()` came after the fact and could not undo work already done (a real double-`ensureDefaultConfig()` race, not just a redundant-Job cosmetic issue). Fixed exactly per the TODO's suggested target: the candidate is `CoroutineStart.LAZY`, which runs no instruction until `Job.start()` is called — only the winner (post-`compareAndSet`) ever calls it, so the loser's `cancel()` always races a not-yet-started job.
- **Verified the fix is real, not just plausible-looking**: reverted `start()` to the old eager-launch code, ran the new 8-thread concurrent-start tests 3 times against it — failed 2 of 3 (`concurrentInitializationStartRunsInitializeExactlyOnce`, then also `losingLazyInitializationJobExecutesNoInstruction` on a second run) — then restored the fix and reconfirmed clean 3/3. This is the same before/after verification discipline used for the P0-009 generation guard and the P1-001 `isBusy`-timing fixes this stage.
- All four named P1-002-B tests added to the existing `AppInitializationCoordinatorTest.kt`, using a real (non-`Unconfined`) `Dispatchers.IO` scope/dispatcher and 8 real OS threads released simultaneously via a `CountDownLatch` — an `Unconfined` dispatcher (used by every other test in this file) never leaves the eager-vs-lazy race window open, so genuine thread-level concurrency was required. `BarrierConfigRepository` counts entries into `ensureDefaultConfig` as the "barrier at the first line of `initialize`" the TODO asked for.
- Validation: full `./gradlew check` PASS; two consecutive full `testDebugUnitTest` runs green (one already-tracked pre-existing flaky-test recurrence — `SetupSaveControllerTest.plaintextIdentityIsWipedOnCancellation`, unrelated to this task — on the release-variant run, confirmed transient by immediate isolated rerun, clean on the following full-suite rerun).

### P0-010 — remove Rust/JNI zero and empty diagnostic fallbacks (HIGH-6) — commits `59a07b9` (Rust), `3f5d3a0` (Android)
- **A/B (Rust)**: `AndroidLogEvent.unix_ms` changed from `u64` to `Option<u64>` — the wall-clock resolution itself (`unix_ms(): Option<u64>`, `resolve_optional_unix_ms`) had already been made optional in a prior fix cycle (comments reference "FIX7 P0-010-B/E/G"), but the struct field it fed into was still a bare `u64`, so the two remaining fallback sites still had to fabricate a value: the JNI invalid-UTF-8 log-JSON fallback hand-wrote `"unix_ms":0` in a raw string literal, and the C-ABI recent-logs-read-failure fallback's rare "log buffer AND clock both unavailable" branch returned an empty `Vec` (indistinguishable from "no logs") instead of a visible error event. New `types::diagnostic_failure_event(message, unix_ms)` (crate-internal) builds the event for both; the JNI fallback now serializes it via `serde_json::to_string` instead of hand-writing JSON (extracted into `jni_bridge::invalid_utf8_log_fallback_json()`, unit tested without a `JNIEnv`), and the C-ABI fallback's two branches collapse into one `recent_logs_failure_fallback(reason, unix_ms)` call (unit tested with both `Some`/`None`).
- **C**: extended the existing FIX7 `no_pre_epoch_panics.rs` workspace guard with a second test scanning for a reintroduced `unix_ms: 0` struct literal, `"unix_ms":0` JSON string, or `None => Vec::new()` fallback in production code — verified against two deliberately reintroduced violations (confirmed each one fails the test, then reverted) before confirming clean. `Option<u64>` already makes the bare struct-literal case a compile error workspace-wide; the guard's real remaining value is catching the JSON-string and `Vec::new()` forms, which the type system can't see.
- **A (Android)**: `LogEvent.unixMs`/`NativeLogEventDto.unixMs` changed to `Long?`, matching the Rust side. `SensitiveDataRedactor.redactLogEvent` and the LazyColumn item key needed no change (the former never touched `unixMs`; the latter's `"${event.unixMs}-$index"` key was already index-stabilized). `LogsScreen`'s `formatLogTimestamp`/`redactedLogsText` (both changed from `private` to `internal` for direct unit testing) now render a null timestamp as a fixed `log_timestamp_unavailable` string resource ("time unavailable") instead of an epoch-derived value; `DiagnosticsRepository`'s JSON-based diagnostics export needed no change (`kotlinx.serialization` already renders a null field as JSON `null`).
- All 7 named P0-010-D tests added (4 Rust: `types.rs`/`c_abi.rs`/`jni_bridge.rs` unit tests plus the extended static guard; 3 Kotlin: a `TunnelRepositoryTest` decode test, and 2 new `LogsScreenTimestampTest` tests).
- Validation: `cargo fmt --all --check` + `cargo clippy --workspace --all-targets` + `cargo test --workspace` PASS after the Rust commit; full `./gradlew check` + two consecutive full `testDebugUnitTest` runs green after the Android commit.

### P1-003 — complete Result, runCatching, filesystem, and raw-log audit (HIGH-9, HIGH-12, MEDIUM-1/2/4) — commit `a3d8a6a`
- A read-only Explore-agent audit against the current codebase (not the FIX7-era snapshot the TODO's inventory bullets were written against) found: Rust workspace already fully clean (no `runCatching`-equivalent gaps); Kotlin's cancellation-first-catch and ignored-filesystem-Boolean requirements were already fully satisfied by earlier FIX7/FIX8 passes (zero gaps for both); the genuine remaining work was 7 production `runCatching` sites, 6 `@CheckResult` gaps, and 6 raw-Throwable-logging sites, plus all 6 P1-003-D tests still missing.
- **A**: replaced all 7 remaining `runCatching` sites with an explicit cancellation-first `try/catch (Exception)` (or, for `NativeLibLoader`'s `System.loadLibrary` call — outside any coroutine — a bare `catch (UnsatisfiedLinkError)`): `RustTunnelBridge.kt`'s native-lib load, `IdentityRepository.kt`'s `readPrivateIdentityFile`, `ConfigRepository.kt`'s `loadSetupInputResult`/`debugAndroidIceModeOverrideOrNull`, `SettingsViewModel.kt`'s `statusJson`/`redactedConfig`, `ForwardsConfigStore.kt`'s `loadForwardsResult`. Deleted the old marker-permitting `RunCatchingInventoryTest`.
- **B**: added `@CheckResult` to `BrokerSecretRepository.captureSnapshot`, `ExactFileSnapshot`'s two capture functions, `SetupPersistenceCoordinator.persist`, `TransactionalResetCoordinator.resetConfiguration` (readPrivateIdentityFile already had it from this task's own A work). This surfaced ~30 existing test call sites across 8 test files that discarded the Result as a bare statement; each was fixed to genuinely consume it — `.getOrThrow()`/an explicit success assertion where the test's own fixture assumes success (including two concurrency tests where this is a real rigor improvement, since a regression breaking either concurrent save would now fail loudly instead of passing silently), or an explicit `isFailure`/`is …Failed` assertion where the test deliberately forces a rollback/cancellation path. No latent bug was uncovered by this — every newly-added assertion passed on the first try.
- **C**: removed raw-`Throwable` logging from `NotificationController.show()`, `ConfigAtomicWrite.kt`'s `AtomicMoveNotSupportedException` fallback, `ForwardsConfigStore.kt`'s `onFailure` block, `ImportExportScreen.kt`'s two share/copy catch blocks, and `TunnelForegroundService.kt`'s `publishErrorSafely` catch — each now logs a fixed prefix plus `SensitiveDataRedactor.redactText(error.message ?: ...)` instead of passing the `Throwable`/`Exception` object itself to `Log.*`, whose stack-trace formatting would otherwise print the full cause chain (potentially including an embedded path or raw content one level down, invisible to a redaction call that only ever sees the top-level message).
- **Genuine security-relevant gap found and fixed while auditing this** (not in the original inventory, surfaced by reasoning about redaction-function scope): `readPrivateIdentityFile`'s `"Identity file not found: $path"` message embedded the user-supplied raw import path verbatim. `SetupIdentityController.importIdentityFromPath()`'s failure handler redacts this message through `SensitiveDataRedactor.redactSecretValues` — deliberately the narrower of the two redaction functions (its own doc comment states it does not apply `redactText`'s identity-path regex, since it's tuned for structured secret values, not arbitrary paths, in user-facing prose). `redactText`'s own identity-path regex is also too narrow to help here regardless (it only matches paths whose last segment is literally `identity`/`identity.toml`/`identity.enc`, not an arbitrary user-chosen path). Rather than widen either redaction function's scope, fixed at the source: `readPrivateIdentityFile` no longer embeds the path in its exception message at all.
- **D**: all 6 named tests added — `productionContainsNoRunCatchingCall` (new `ProductionRunCatchingAuditTest.kt`, a zero-tolerance static scan replacing the deleted marker-permitting test), `nativeLibraryLoadNormalizesOnlyUnsatisfiedLinkError` (new `RustTunnelBridgeNativeLoadTest.kt` — exercises the real `System.loadLibrary` failure in the plain JVM test process, no mocking, since there genuinely is no native `.so` on the unit-test library path), `fatalErrorFromParserOrPropertyReadPropagates` and `securityExceptionFromEachResultApiBecomesFailureOrDocumentedThrow` (added to `IdentityRepositoryTest.kt`, using `readPrivateIdentityFile`'s injectable `readText` seam added in this task's A work), `rawPrivatePathSentinelNeverAppearsInProductionDiagnosticStateOrLogs` (added to `AllViewModelFailureRedactionTest.kt`, following that file's existing sentinel-injection convention, targeting the exact `importIdentityFromPath` gap fixed above), `authoritativeFilesystemOperationsContainNoUncheckedBooleanResult` (new `AuthoritativeFilesystemBooleanAuditTest.kt`, a static scan confirming zero `mkdirs()`/`delete()`/`setReadable()`/`setWritable()` calls remain in production — already true before this task, now guarded against regression).
- Two detekt findings surfaced mid-task and were fixed by restructuring, not suppressing: a `SwallowedException` in `ConfigRepository.kt`'s debug-only ADB-property read (renamed the catch variable to `expected`, detekt's own documented escape hatch for a deliberately-ignored exception, not a suppression annotation) and a `LongMethod`/`TooManyFunctions` pair in `ImportExportScreen.kt`'s `PublicIdentityShareRow` (the share/copy actions' duplicated try/catch-and-redact logic was consolidated into one shared `private val` of function type — a property, not a `fun`, to stay under the per-file function-count threshold).
- Validation: full `./gradlew check` PASS; two consecutive full `testDebugUnitTest` runs green (one already-tracked pre-existing flaky test, `SetupSaveControllerTest.plaintextIdentityIsWipedOnCancellation`, reconfirmed pre-existing by isolating it against a clean `git stash`d `master` baseline — failed there too, unrelated to this task). `cargo fmt --all --check` + `cargo clippy --workspace --all-targets` PASS (no Rust changes were needed).

### P1-004 — close missing/misleading production-path tests and CI nondeterminism (P2-001 gaps, incomplete FIX7 signoff, CI flakes) — commit `83d074c`
- A read-only Explore-agent audit against the CURRENT codebase (not the FIX7-era snapshot the TODO's language was written against) found: `ForwardConfigurationCoordinatorTest`'s forward-cleanup tests already assert exact bytes (no gap); 8 of 9 existing "RollbackIncomplete"-named tests already inject failure specifically into a restore step (no gap); one misleadingly-named test (`ForwardsRepositoryTest.rollbackRestoresExactState`, which only checked the new entry was absent, not that the prior list/bytes were restored); two config-import cleanup tests using weaker `String`/`readText()` equality instead of byte-exact comparison; the P0-009 service-recreation quarantine test bullet already satisfied by existing work; ~15 files independently reimplementing the same ad-hoc polling loop; and all 4 named P1-004-D tests genuinely missing.
- **A**: `ImportExportServiceTest`'s `configImportCleanupFailurePerformsNoAuthoritativeConfigWrite`/`configImportWritePostMoveFailureRestoresPreviousConfigBytes` switched to `writeBytes`/`readBytes`/`assertArrayEquals`. `ForwardsRepositoryTest.rollbackRestoresExactState` renamed `rollbackRestoresExactPriorListAndFileBytes`, now captures the prior in-memory list AND on-disk bytes before the mutation and asserts both are restored exactly (not just "the new entry is gone").
- **C (root-cause, not just re-record) — two genuinely flaky tests fixed**:
  - `TunnelForegroundServiceVerificationTest.startVerificationCleanupFailureEntersRuntimeQuarantine`: its `waitForCondition` polled `serviceState == Error && lastError != null` — a WEAKER predicate than the canonical-code assertion that followed. `enterNativeRuntimeQuarantine` makes two separate, non-atomic `setLocalError` calls (the narrow diagnostic code, then the canonical `native_runtime_quarantined` code) on another thread; the poll could observe the intermediate window and declare success before the final code landed, racing the subsequent `assertEquals`. Fixed by polling for the exact canonical code instead of a weaker proxy. 5/5 clean in isolation after the fix (this test was also flagged as a NEW recurrence — "fifth pre-existing flaky test" — during P0-009 earlier this session; this fix root-causes and closes it).
  - `SetupSaveControllerTest`'s three cancelled-config-write tests (`plaintextIdentityIsWipedOnCancellation`, `configWriteCancellationPropagatesAndDoesNotReportFailureOrSuccess`, `cancellationNeverReportsConfigurationSavedOrOrdinarySaveFailure`) used a fixed `repeat(20) { idle(); delay(5) }` "settle" loop instead of an identified completion event — this was the MOST-recurring tracked flaky test all session, and reproduced 5/5 in isolation by the time this task started (not merely intermittent — the 100ms budget had become insufficient once P1-001 added more suspension points to the save path via `SetupOperationCoordinator.runGuarded`). Replaced with a real completion signal: a new `awaitCancelledSaveToSettle` helper waits for `isBusy` to become `true` then `false` (the wipe/rollback happens inside `runGuarded`'s block before its own `finally` re-stamps busy, so `false` unambiguously means "fully settled, including any cancellation-path cleanup"). 5/5 clean in isolation after the fix, then 3/3 clean for the whole class.
  - The other three previously-tracked flaky tests (`TunnelForegroundServiceInitializationRaceTest.startWhileExactlyInitializingDoesNotCallNative`, `TunnelForegroundServiceOrderingTest.nativeFailureConsumesPendingPolicyRetryAndResumesExactlyOnce`, `TunnelForegroundServiceRuntimeSafetyRecreationTest.reporterFailureCannotPreventSharedQuarantineTransition`) did not reproduce in 5 isolated reruns each; code review of the latter's race-orchestration found a plausible (not confirmed) missing synchronization point between "the second network event is recorded as pending" and "the blocked native call is released," but a fix could not be verified without being able to reliably reproduce the failure first, so it was left as-is rather than applying an unverified change. These remain tracked, not newly fixed.
  - One additional flaky recurrence (unidentified — the test report was overwritten before it could be inspected) surfaced once across roughly 8 total full-suite reruns performed during this task's validation; every other run in that same set was clean, including the two required consecutive final runs.
- **C (shared helper)**: new `TestAwait.kt::awaitCondition` (two overloads: value-returning with an explicit predicate, and a boolean-predicate convenience form) is the one shared implementation behind the polling loop every one of ~14 files' own `awaitXyz`/`awaitCondition` wrapper now delegates to, instead of each reimplementing `withTimeout(5_000) { while(...) { idle(); delay(10) } }` independently. Per-file wrapper names/signatures are unchanged (only their internals were deduplicated), so no test call site elsewhere needed to change. A timeout now surfaces a specific `AssertionError` naming what was being waited for, instead of kotlinx.coroutines's generic timeout message.
- **B/D**: `lateStartupCompletionAfterDestroyIsRejectedByRealGenerationPath` — the FIX7 P2-001-B deviation (a literal "StartupCompleted lands in the gap between `coordinator.stop()` closing the queue and `cancelStartupJobAndJoinLocked()` reaching the job" thread race) remains unforceable via literal interleaving for the same reason recorded then (releasing a pre-native-call block after cancellation is requested makes the coroutine observe the cancellation instead, per `TunnelForegroundServiceStopFailureTest.pendingRetryThenDestroyDoesNotRestart`), and adding a brand-new production-only pause point for just this one window would introduce a static-test-hook-in-production pattern this codebase has consistently avoided elsewhere in favor of real DI seams. Instead, the test drives the real `ServiceCoordinatorOperations.handleStartupCompleted` collaborator directly with a stale generation — the exact guard the race would exercise (`if (service.lifecycleGeneration.get() != generation) return`), proven directly rather than merely inspected. `TunnelForegroundService.coordinatorOps` changed from `private` to `internal` so the test can call it and assert immediately with no intervening barrier command — every other lifecycle command's own successful handler also resets `lastError` as a side effect of publishing its own clean state (confirmed for `setPolicyBlocked` and an explicit `Stop`'s `repository.stop()` success), which would silently mask whatever a wrongly-processed stale completion had set, making the test pass regardless of whether the guard worked; an earlier version of this test that used a `submitLifecycleCommand` + STOP-barrier design was caught by exactly this — verified vacuous by deliberately disabling the guard (`if (false) return`) and observing the original design still passed, then rewritten to the direct-call form and reconfirmed: fails when the guard is disabled, passes 3/3 when restored.
- **B/D**: `productionReporterThrowCannotPreventQuarantineOrProcessorFailureTruth` (added to `TunnelForegroundServiceVerificationTest.kt`) — a real `NotificationController` (only its `notifyAction` lambda swapped, the same seam P0-009's `TunnelForegroundServiceRuntimeSafetyRecreationTest` uses) that throws on every call, exercising the verification-failure/cleanup call site's own `enterNativeRuntimeQuarantine -> reporter.publishErrorSafely`, distinct from P0-009's service-recreation/explicit-stop-failure call site. Asserts the canonical quarantine code and sticky cleanup diagnostic both still land, and that the lifecycle command processor itself survives (a later STOP command is still accepted and processed) — not just that one field happened to be set before the throw. 3/3 clean in isolation.
- **B/D**: `stopWhileListeningWithoutPeerReportsStoppedNotErrorInstrumentation` (new `androidTest`, run against a live emulator, `Medium_Phone_API_36.0(AVD)`) — `RecordingBridge`'s `getStatusJson()` never sets `active_session_count`, so a successful offer start is genuinely reported by the real `TunnelRepository`/`mapNativeServiceState` mapping as `Listening` (offer running, zero connected peers), not `Connected` — verified via `dumpsys`-level reasoning about the DTO, then confirmed empirically. A stop from `Listening` is asserted to report `Stopped`, never `Error`. Passed in isolation on 2 separate runs; a full-class run of the pre-existing 17-test `TunnelForegroundServiceInstrumentationTest` showed 9 unrelated pre-existing failures (all timing out waiting for `startOfferCalls >= 1` after `startForegroundService`) — confirmed unrelated to this change (`git diff --stat -- android/app/src/main` was empty at the time), a pre-existing environment/emulator-load issue in this already-non-CI-gated suite, not investigated further (out of scope: `androidTest` is explicitly non-required CI per this repo's own convention).
- **B/D**: `meteredToUnmeteredTransitionPausesAndResumesAccordingToPreferenceE2E` (new `tests/e2e/android_metered_transition.sh`, same local/manual tier as `android_smoke.sh`) — drives the wizard to `Listening`, then attempts to force the active network metered. Two mechanisms were tried and rejected before finding one that works: `cmd netpolicy set metered-network` changes a policy-level override but not the live `NetworkCapabilities` a `NetworkCallback` observes (confirmed via `dumpsys connectivity` still showing `NOT_METERED` afterward); `svc wifi disable` only flips the `WifiManager` state flag without tearing down the Wi-Fi `NetworkAgent` on this emulator image (confirmed via `dumpsys connectivity` still listing an active `ni{WIFI` agent, and once produced a spurious runtime-quarantine `Error` from a connectivity glitch during the half-torn-down state). `cmd wifi set-wifi-enabled disabled/enabled` genuinely removes/re-adds the Wi-Fi agent (confirmed: `dumpsys connectivity` then lists only the cellular agent, whose capabilities lack `NOT_METERED`) and is what the script uses. **Observed environment limitation, documented rather than hidden**: on this repo's dev AVD image, Wi-Fi sometimes auto-reconnects within 1-2s regardless of the disable command, producing a rapid drop-then-return flap while the tunnel is active instead of a stable metered window; the app's stop-like-failure quarantine safety net (already proven correct by other tests this task and P0-009) can legitimately trip on that flap instead of a clean pause — a defensible reaction to a confusing rapid-flap signal, not a reproduced app defect, but it means the script does not reliably pass on every run of this specific emulator. Kept as a local/manual artifact (not required for signoff), same as `android_smoke.sh`; a physical device with a genuinely stable metered toggle is expected to exercise the intended clean transition.
- The P0-009 service-recreation quarantine bullet was cross-checked against the already-committed `TunnelForegroundServiceRuntimeSafetyRecreationTest` (`SHA: 3643841`) and ticked with that reference — no new work needed.
- Validation: full `./gradlew check` PASS (twice, `--rerun-tasks`); `cargo fmt --all --check` + `cargo clippy --workspace --all-targets` PASS (no Rust changes). Both root-caused fixes verified via the established revert-and-confirm-fails discipline. Roughly 8 full `testDebugUnitTest` reruns performed across this task's validation (root-cause confirmation + shared-helper-refactor verification + final signoff); all clean except one unidentified single-run flaky recurrence (report overwritten before inspection), immediately clean on retry — the two required final consecutive full runs were both green.

### P0-001-C gap discovered while scoping Stage E — see the P0-001-C entry above
Recorded there (commit `4bbd63e`), not repeated here — flagged mid-Stage-E audit, confirmed with the user before implementing (see the conversation's own record of that check-in), then fixed and its TODO boxes ticked retroactively alongside the rest of P0-001.

### P2-001 — permanent static enforcement and negative fixtures — commits `4443101` (Android), `7826953` (CI)
- A read-only Explore-agent audit first mapped exactly what P2-001 needed against the CURRENT codebase (not the FIX7-era snapshot the TODO's own inventory commands were written against), since several of its bullets already overlap with FIX8 P1-003/P1-004's own static-scan additions this session. Found: `runCatching`/filesystem-Boolean bans already permanently enforced (P1-003); the setup-controller mutation ban was NOT yet true (blocked on the just-discovered P0-001-C gap, see above); 6 concrete `@CheckResult` gaps; zero `catch (Throwable)` clauses exist (so "allowlist the one exception" simplifies to "enforce zero"); the negative-fixture bullet was a genuine, unaddressed gap requiring new lint-tests plumbing; all three P2-001-B snapshot/fallback invariants hold in production today but had no enforcement test; P2-001-C (Rust) and most of P2-001-D (CI) were already fully satisfied.
- **A (`@CheckResult`)**: added to `TunnelRepository.start()`, `ForwardsStore.loadForwardsResult()`, `MutationHelpers.mutationResult()`, and `ConfigAtomicWrite.kt`'s `writeConfigAtomicallyWith()`/`atomicReplaceBytesWith()`. Three function-typed properties the audit also flagged (`ConfigRepository.replaceConfigTransactionally`, `UnverifiedStartContext.stop`, a private `SetupPersistenceCoordinator` constructor param) were deliberately left unannotated — Android Lint's `CheckResultDetector` is documented to act on method/getter declarations, and annotating a function-typed *property* would invoke `Function1.invoke` at the call site rather than a checked method, so the annotation likely wouldn't be enforced there anyway; adding it would be a false signal, not real protection. One existing test (`AtomicConfigWriteTest`'s cancellation-propagation case) surfaced a genuine `IgnoredReturnValue` finding once `writeConfigAtomicallyWith` gained the annotation — fixed by consuming the (unreachable, since the call always throws in that test) return value into a `val` used in the failure message.
- **A (setup-controller ban, cleanup-Throwable-catch)**: `SetupControllerNoAuthoritativeMutationTest` (zero-tolerance scan of `viewmodel/Setup*.kt` for `storeEncryptedIdentity(`/`upsertWithReceipt(`/`deleteWithReceipt(`) and `CleanupCompositionThrowableCatchAuditTest` (zero-tolerance scan for `catch (Throwable)` anywhere in production) — both follow the established `ProductionRunCatchingAuditTest`-style line-scan pattern from P1-003.
- **A (negative fixture — the one genuinely new-plumbing item)**: added `com.android.tools.lint:lint-tests` + `lint-checks` (31.5.2, matching this project's AGP 8.5.2 per lint's own API-version-tracks-AGP convention) as `testImplementation` dependencies. `CheckResultEnforcementFixtureTest` runs the REAL, built-in `CheckResultDetector` (not a reimplementation) against a small in-memory Kotlin fixture (plus a Java stub for the `androidx.annotation.CheckResult` annotation type, needed since the fixture is otherwise fully isolated from this project's real classpath) — one test proves an ignored `@CheckResult` call is flagged, a second proves a consumed one is clean. Took some iteration to get right: `lint-tests` alone doesn't expose the actual detector classes at compile time (only the `TestLintTask` infrastructure) — needed the separate `lint-checks` dependency; and the detector's own built-in default severity is `Warning`, not `Error` — this project's promotion to error is a plain `build.gradle.kts` `lint { error += "CheckResult" }` DSL setting that a standalone fixture run (independent of this module's build config) doesn't inherit, so the test asserts `expectWarningCount(1)` and documents why, rather than `expectErrorCount`. Replaced the `build.gradle.kts` lint block's own comment ("verified by a temporary deliberate bare call" — exactly the kind of historical, non-committed verification the TODO says not to rely on) with a pointer to this test.
- **B**: `SnapshotAndCandidateBlockEnforcementTest` covers all three P2-001-B invariants. The third (no authoritative config/forwards write inside a `withCandidateFile`/`withTemporaryDirectory` block) needed real brace-depth matching, not a line-level regex — implemented as a small self-contained scanner (finds each genuine call site, locates its `{`, walks forward tracking `{`/`}` depth to find the matching `}`, then scans only that span for `writeConfigAtomically`/`replaceConfigTransactionally`/`saveForwards`/`saveConfig`). Verified by deliberately moving `deps.configRepository.replaceConfigTransactionally(candidate)` inside `ImportExportService.kt`'s `withCandidateFile` block, confirming the test fails, then reverting and confirming clean — the same revert-and-confirm-fails discipline used throughout this session for anything non-trivial enough to risk being vacuous.
- **C**: no code changes needed — `cargo test -p p2p-core --test no_pre_epoch_panics` (2 passed) and `cargo test -p p2p-mobile recent_logs_and_clock_double_failure_returns_visible_untimed_error_event` (1 passed) both confirmed still green, exactly as built in P0-010.
- **D**: `.github/workflows/ci.yml` already ran `./gradlew --no-daemon check` and full-feature/target Rust fmt/clippy/test, and already had no auto-rerun-and-report-success pattern anywhere — confirmed by direct reading, no change needed. The one real gap: the `android` job's ktlint/detekt/lint HTML reports and JUnit XML results were never uploaded anywhere, so a CI failure's actual diagnostic detail lived only in the raw (often truncated) step log. Added an `if: failure()` `actions/upload-artifact` step uploading `android/app/build/reports/` and `build/test-results/` (14-day retention) at the end of the `android` job. The Rust jobs' failures are already fully visible via console output (fmt/clippy/test print directly to stdout, which GitHub Actions retains by default), so no equivalent artifact step was added there.
- Validation: full `./gradlew check` PASS (`--rerun-tasks`, twice); two consecutive full `testDebugUnitTest` runs green. `cargo fmt --all --check` + `cargo clippy --workspace --all-targets` PASS (no Rust production changes this task). The CI workflow YAML change was validated with `python3 -c "import yaml; yaml.safe_load(...)"` (no push/CI run performed — pushing/triggering CI was not requested).

### P2-002 — final validation and immutable signoff (in progress)

**P2-002-A — repository and handoff state**: input files confirmed present (`FIX8_SPEC.md`, `FIX8_TODO.md`, FIX7 code review, handoff manifest, `FIX8_RESPONSES.md`); this implementation report exists and is current; no handoff document references an unavailable file. `git rev-parse HEAD`/branch/status are recorded at the very end of signoff (see the final entry below), not here, since further docs-only commits follow this validation work.

**P2-002-B — focused Android validation**: one explicit command covering every class named in the TODO plus every FIX8-added static-scan/fixture test:
```bash
cd android && ./gradlew testDebugUnitTest --rerun-tasks \
  --tests "*SetupViewModelTest" --tests "*SetupIdentityControllerTest" --tests "*SetupIdentityActionsTest" \
  --tests "*SetupForwardsControllerTest" --tests "*SetupSaveControllerTest" --tests "*SetupValidationWorkspaceIntegrationTest" \
  --tests "*ConfigurationMutationCoordinatorTest" --tests "*ConfigurationMutationIntegrationTest" --tests "*ConfigRepositoryTest" \
  --tests "*ExactFileSnapshotTest" --tests "*ExactFileStateTest" --tests "*MutationHelpersTest" \
  --tests "*SetupPersistenceCoordinatorTest" --tests "*SetupPersistenceCoordinatorExactBytesTest" --tests "*SetupPersistenceCoordinatorForwardsTest" \
  --tests "*ForwardsConfigStoreTest" --tests "*ForwardsRepositoryTest" --tests "*ForwardConfigurationCoordinatorTest" \
  --tests "*ImportExportServiceTest" --tests "*ImportExportViewModelTest" --tests "*ForwardsViewModelTest" \
  --tests "*TransactionalReset*Test" --tests "*SettingsViewModelTest" --tests "*NetworkPolicyViewModelTest" \
  --tests "*IdentityRepositoryTest" --tests "*IdentityPersistenceAtomicityTest" --tests "*BrokerSecretRepositoryTest" \
  --tests "*AppInitializationCoordinatorTest" --tests "*TunnelRepositoryTest" --tests "*TunnelRepositoryInvalidStatusTest" \
  --tests "*TunnelLifecycleCoordinatorTest" --tests "*TunnelForegroundService*Test" \
  --tests "*SetupWizardNoIdentityMutationTest" --tests "*SetupControllerNoAuthoritativeMutationTest" \
  --tests "*ProductionRunCatchingAuditTest" --tests "*AuthoritativeFilesystemBooleanAuditTest" \
  --tests "*CleanupCompositionThrowableCatchAuditTest" --tests "*SnapshotAndCandidateBlockEnforcementTest" \
  --tests "*CheckResultEnforcementFixtureTest" --tests "*AtomicConfigWriteTest" --tests "*SetupDraftOperationCoordinationTest" \
  --tests "*SetupSaveAuthorizedKeysTest" --tests "*AllViewModelFailureRedactionTest"
```
PASS, zero failures (this run predates the P2-002 stop-verification fix below; re-confirmed clean again as part of the full-suite reruns after that fix landed).

**P2-002-C — full Android validation**: `ktlintCheck`, `detekt`, `lintDebug`, three consecutive full `testDebugUnitTest --rerun-tasks`, `assembleDebug`, `check` — all PASS (run twice: once before, once after the P2-002 stop-verification fix; the fix's own two required consecutive full reruns are the ones that count as this task's final evidence).

**P2-002-D — Rust validation**: `cargo fmt --all -- --check` PASS; `cargo clippy --workspace --all-targets --all-features -- -D warnings` PASS, zero warnings; `cargo test --workspace --all-features` PASS (30 test binaries, all `ok`, 0 failed); `cargo test -p p2p-daemon --test real_broker_tunnel --all-features` PASS (`full_tunnel_over_real_tls_broker ... ok`, Docker available, executed rather than self-skipped). Null-timestamp/double-log-failure tests: `no_pre_epoch_panics.rs`, `c_abi.rs::recent_logs_and_clock_double_failure_returns_visible_untimed_error_event` (FIX8 P0-010/P2-001-C).

**P2-002-E — Docker and emulator E2E**:
- `tests/e2e/docker/run.sh` (full docker-compose data path over a real TLS broker) — PASS, "PASS — full tunnel delivered target content over a real TLS broker".
- `tests/e2e/docker/stop_lifecycle.sh` — PASS, `docker stop` returned after 2s (well under the 10s grace period), both daemons drained and exited 0.
- `tests/e2e/android_smoke.sh` against the live `Medium_Phone_API_36.0(AVD)` emulator (real broker.emqx.io) — **found a real, reproducible (3/3) production bug on first run**: tapping Stop after reaching `Listening` consistently failed with `"Native stop returned success but final state was not verified as Stopped"`, driving the app into runtime quarantine, even though the app's own on-device Logs screen showed the native runtime had already logged `"runtime completed"`/`"runtime stopped"`. Delegated a read-only investigation (Explore agent) into `crates/p2p-mobile`/`crates/p2p-daemon` to rule out a genuine Rust-side race before touching Kotlin — confirmed the Rust side is fully synchronous and correct (`stop_with_grace_period` `block_on`s the spawned task's `JoinHandle`, which cannot resolve before the task's own final status update releases the same mutex `getStatusJson()` reads afterward — a real happens-before guarantee, not a heuristic). Root cause was on the Kotlin side: `TunnelRepository.kt`'s `reportsVerifiedStop` and `toTunnelStatus` both required native `mode` to resolve to a non-null `TunnelMode` *before even checking `state`* — but the real native runtime always reports `mode=null` alongside a genuine stopped status (deliberately, so the UI never shows a stale offer/answer label after stopping), so a real stop's status payload could never satisfy either check. This made the FIX8 P0-009 "verified explicit STOP clears quarantine" guarantee structurally unreachable in production — every real stop looked like a verification failure. Fixed both call sites to check `state == "stopped"` directly (mode is irrelevant to that case; `mapNativeServiceState`'s own "stopped" branch already ignores it), `SHA: 64ec808`. The existing unit tests didn't catch this because their "stopped" fixtures always passed a non-null mode string (unlike the real native contract) — fixed those fixtures (which reproduces the bug against the pre-fix code, confirmed via the established revert-and-confirm-fails discipline) and corrected one test (`missingModeReturnsSchemaError`) whose own "stopped + missing mode = schema error" expectation was itself the incorrect assumption that let this ship — split into a non-terminal-state case (still a genuine schema error, mode really is needed there) and a new `missingModeIsNotASchemaErrorWhenStopped` case. Re-ran `android_smoke.sh` after the fix: clean PASS (`Listening` → `Stop` → `Stopped`). Manually verified via `adb`/`uiautomator` that a subsequent Start after that Stop reaches `Running`/`Listening` cleanly (no residual quarantine).
- `tests/e2e/android_tunnel_e2e.sh` (Android offer → real WebRTC → dockerized `p2p-answer`, real bytes) — hit UI-automation input flakiness on the first few attempts (a known characteristic of this local/manual script per its own documentation: base64 identity paste occasionally corrupted, and one run's app process wasn't found afterward, consistent with an ANR/kill under repeated-launch load rather than an app defect) — passed cleanly on a subsequent attempt: "PASS: marker delivered THROUGH the Android offer tunnel to the dockerized answer", "verified data-plane probe: answer received PING and replied PONG".
- `tests/e2e/android_metered_transition.sh` (FIX8 P1-004) — the documented environment limitation from that task recurred (this dev AVD's Wi-Fi auto-reconnect races the disable command); not re-investigated further here, same "local/manual, not required for signoff" status as `android_smoke.sh`.
- Quarantine-survives-recreation and verified-explicit-stop-clears-it: primary evidence is `TunnelForegroundServiceRuntimeSafetyRecreationTest` (FIX8 P0-009, `SHA: 3643841`), which already constructs two real `ServiceController` instances sharing one process/dependency graph — exactly the "same process/test application" shape this bullet asks for; the live on-device bug-then-fix cycle above additionally reproduced the full quarantine → verified-stop → clear → successful-restart cycle for real, unintentionally, which is stronger evidence than a synthetic trigger.

**P2-002-F/G/H**: recorded in the next report entry, after CI/final-inventory/acceptance work completes.
